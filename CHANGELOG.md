# Changelog

[中文](CHANGELOG_ZH.md)

All notable changes to evap-shield are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). This project groups changes by date rather than semantic version — it's a script toolkit, not a registry-published package.

## 2026-07-22

### Changed

- Bumped the tested badge to **2.1.217**. Claude Code updated 2.1.216 → 2.1.217 — a consecutive version step, so the two-binary diff brackets no skipped build (`~/.local/share/claude/versions/` now holds 215, 216, 217; 2.1.203, 2.1.209, and 2.1.213 remain the builds never installed on this machine). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.216 — a ±400-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), now **thirteen** consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210, 2.1.211, 2.1.212, 2.1.214, 2.1.215, 2.1.216, 2.1.217) carrying the same local names. Build provenance: the factory binary grew 1,231,200 bytes (249,225,584 → 250,456,784) and the parser site drifted +1,034,465 bytes (219,364,361 → 220,398,826); the Bun banner reads `Bun v1.4.0` unchanged. Anthropic's 2.1.217 changelog (20 entries, mechanical `grep -c` over a direct fetch; npm shows it published 2026-07-21T19:55Z) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser. The entries that *sound* closest sit in different layers: "Fixed a memory leak where truncated MCP tool outputs kept the full untruncated result in memory" is output-buffer bookkeeping, not parsing; "Fixed `--resume`/`--continue` and `/resume` failing with a TypeError when a transcript has a malformed attachment entry" is transcript-file loading robustness, not the wire parser; the release's bulk is subagent governance (a concurrency cap, a no-nested-spawns default, `--max-budget-usd` now halting background agents) plus Windows/background-session repair and UI polish (emoji shortcode autocomplete, screen-reader fixes). A strings diff (5,386 added / 2,902 removed) shows 2 charCode/codePoint/tokenizer-flavored lines in the added set — both from a single new template's backslash ban (`fromCharCode(92)` in a comment plus a `json.indexOf(String.fromCharCode(92))` check), content sanitization, not the streaming parser (the zero-streak from 2.1.216 ends, but both lines are identified); the 28 generic parse hits sit in DOMParser HTML round-trips and a VM-realm `JSON.parse` error-detail bridge — none the VH1 site; theme counts (agent 56, transcript 24, subagent 22, budget 11, worktree 8) line up with the changelog's 20 entries. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `5840c777fd47…` → patched `cda2b45d5589…`, completed 2026-07-22T04:28:53Z), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 51005, to `~/.local/share/claude/versions/2.1.217`, inode 60393820, launched 23 seconds after the patch completed — the only live claude process on the machine at check time) — patched dogfooding. (Disk-level footnote: the re-sign shrank the file the usual 1,184 bytes — factory CodeDirectory 15,256+7 hash slots, 488,549 bytes, plus a 9,046-byte Developer ID certificate chain, replaced by an ad-hoc 15,256+2 — making **eight** consecutive lean factory layouts: 2.1.208, 210, 211, 212, 214, 215, 216, 217.) This is the **twenty-seventh** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208, 210, 211, 212, 214, 215, 216) to leave VH1 unfixed.

## 2026-07-21

### Changed

- Bumped the tested badge to **2.1.216**. Claude Code updated 2.1.215 → 2.1.216 — a consecutive version step, so the two-binary diff brackets no skipped build (`~/.local/share/claude/versions/` now holds 214, 215, 216; 2.1.203, 2.1.209, and 2.1.213 remain the builds never installed on this machine). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.215 — a ±400-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), now **twelve** consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210, 2.1.211, 2.1.212, 2.1.214, 2.1.215, 2.1.216) carrying the same local names. Build provenance: the factory binary grew 2,101,248 bytes (247,124,336 → 249,225,584) and the parser site drifted +1,817,780 bytes (217,546,581 → 219,364,361); the Bun banner reads `Bun v1.4.0` unchanged. Anthropic's 2.1.216 changelog (40 entries, mechanical `grep -c` over a direct fetch — back to a full page after 2.1.215's single entry; npm shows it published 2026-07-20T20:19Z) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser. The entries that *sound* closest all sit in different layers: "Fixed Bash command parsing of non-ASCII characters to match real shell word boundaries" is the Bash permission engine's shell word-splitting, not JSON; "Fixed a slowdown in long sessions where message normalization cost grew quadratically" is a performance fix in message bookkeeping; "Fixed cloud sessions dropping the in-flight message when the session's container restarts mid-turn" is cloud-session resume; the release's bulk is worktree/background-session repair and permission hardening (invisible-Unicode PowerShell commands, Windows network paths, compound statements with redirects). A strings diff (5,089 added / 3,899 removed) is the cleanest read in several entries: **zero** charCode/codePoint/tokenizer-flavored lines in the added set (a repeat of 2.1.206/2.1.207's zero, after 2.1.214's 142 and 2.1.215's 130 re-segmentation lines), and the 15 generic parse hits sit in plugin eval, proxy response, the Storybook adapter, cron prompt parsing, and MCP config loading — none the VH1 site; theme counts (agent 87, worktree 36, permission 24, symlink 12, daemon 11, sandbox 10, oauth 10, chart palette 10) line up with the changelog's 40 entries. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `d01b49210d72…` → patched `c7d83501e003…`, completed 2026-07-21T02:35:36Z), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 21973, to `~/.local/share/claude/versions/2.1.216`, inode 60175821, launched 40 seconds after the patch completed; the machine's other live claude process, PID 21681, holds the same patched inode) — patched dogfooding. (Disk-level footnote: the re-sign shrank the file the usual 1,184 bytes — factory CodeDirectory 15,181+7 hash slots, 486,149 bytes, plus a 9,047-byte Developer ID certificate chain, replaced by an ad-hoc 15,181+2 — making **seven** consecutive lean factory layouts: 2.1.208, 210, 211, 212, 214, 215, 216.) This is the **twenty-sixth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208, 210, 211, 212, 214, 215) to leave VH1 unfixed.

## 2026-07-20

### Changed

- Bumped the tested badge to **2.1.215**. Claude Code updated 2.1.214 → 2.1.215 — a consecutive version step, so the two-binary diff brackets no skipped build (`~/.local/share/claude/versions/` now holds 212, 214, 215; 2.1.203, 2.1.209, and 2.1.213 remain the builds never installed on this machine). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.214 — a ±400-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), now **eleven** consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210, 2.1.211, 2.1.212, 2.1.214, 2.1.215) carrying the same local names. Build provenance: the factory binary grew just 32,832 bytes (247,091,504 → 247,124,336) — the smallest size change this ledger has recorded — and the parser site drifted +24,992 bytes (217,521,589 → 217,546,581); the Bun banner reads `Bun v1.4.0` unchanged. Anthropic's 2.1.215 changelog is a **single entry** — the second single-entry release after 2.1.209; npm shows it published 2026-07-19T00:53Z, a day after 2.1.214 — "Claude no longer runs the `/verify` and `/code-review` skills on its own": skill-invocation policy, model-facing surface, nowhere near tool-call parsing, JSON tokenization, or the streaming parser. A strings diff (12,683 added / 5,948 removed — large churn for a 33 KB size step and a one-line changelog, which is re-segmentation at work: `strings` re-cuts shifted byte runs into "new" lines) shows 130 charCode/codePoint/tokenizer-flavored lines in the added set, but sampling finds the familiar crop: the same ajv schema-codegen template block (its `w_` naming carried over from 2.1.214's diff), a PEM parser, PowerShell launcher strings, highlight.js grammars; the generic parse/stream hits sit in acorn-style module parsing, Azure SDK plumbing, and database retention sweeps — none the VH1 site; the verdict rests on the structural anchor's byte-for-byte window read, not the strings diff. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `90608b5c5ab5…` → patched `c09a92f80424…`, completed 2026-07-20T00:15:27Z), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 4848, to `~/.local/share/claude/versions/2.1.215`, inode 60022893, the SessionStart check firing 23 seconds after the patch completed; the machine's one other live claude process, PID 6758, holds the same patched inode) — patched dogfooding. (Disk-level footnote: the re-sign shrank the file the usual 1,184 bytes — factory CodeDirectory 15,053+7 hash slots, 482,053 bytes, plus a 9,047-byte Developer ID certificate chain, replaced by an ad-hoc 15,053+2 — making **six** consecutive lean factory layouts: 2.1.208, 210, 211, 212, 214, 215.) This is the **twenty-fifth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208, 210, 211, 212, 214) to leave VH1 unfixed.

## 2026-07-18

### Changed

- Bumped the tested badge to **2.1.214**. Claude Code updated 2.1.212 → 2.1.214 — skipping 2.1.213, which was never installed on this machine and, unlike 2.1.209 (a single-entry changelog), has **no changelog section at all**: npm shows 2.1.213 published 2026-07-17T22:26Z and superseded by 2.1.214 just 107 minutes later (2026-07-18T00:13Z), so there is nothing to bracket and the two-binary diff runs 212 ↔ 214 directly (`~/.local/share/claude/versions/` now holds 211, 212, 214; 2.1.203, 2.1.209, and 2.1.213 are the builds never installed here). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.212 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), which now makes **ten** consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210, 2.1.211, 2.1.212, 2.1.214) carrying the same local names. Build provenance: the factory binary grew 2,560,992 bytes (244,530,512 → 247,091,504) and the parser site drifted +2,377,380 bytes (215,144,209 → 217,521,589); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.214 changelog (47 entries, mechanical `grep -c` over a direct fetch) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser. The entries that *sound* closest all sit in different layers: "Fixed stream-json output truncation at exit for slow-reading SDK/pipeline consumers" (the exit drain now scales with queued bytes) is CLI stream-json print-mode stdout, the same layer as 2.1.208/210/211; "Added a periodic progress heartbeat for long-running tool calls" is render-layer UI; the release's headline cluster of permission-check hardening (single-segment `dir/**` rules, fd-redirect forms, >10,000-char commands, zsh subscripts in `[[ ]]`, PowerShell 5.1 bypasses) is the permission engine; and the EndConversation tool is model-facing surface. One entry earns an operational note for this repo rather than a ledger line: "Fixed hooks with exit code 2 not blocking as documented when the hook's stdout JSON fails schema validation" touches the hook engine our PreToolUse guard runs on — but `evap-shield.sh`'s block path prints to **stderr** only and exits 2 with nothing on stdout, so it was never on the broken path and its behavior is unchanged. A strings diff (10,892 added / 11,116 removed — the first entry in this ledger where removals outnumber additions, even as the binary grew 2.4 MB; `strings` counts distinct lines, not bytes) shows 142 charCode/codePoint/tokenizer-flavored lines in the added set, but they are the familiar re-segmentation crop: the same pre-existing library spans surface in both this diff and 2.1.212's (an ajv schema-codegen template block — identifiable across the pair by its `H_` → `w_` rename — a PEM parser, PowerShell launcher strings, highlight.js grammars), plus one named-pipe `name.charCodeAt(0) === 0` check of the same kind ruled out at 2.1.208; the verdict rests on the structural anchor's byte-for-byte window read, not the strings diff. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `59796dd18e9d…` → patched `69d048ad2b16…`, completed 2026-07-18T04:15:44Z), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 1800, to `~/.local/share/claude/versions/2.1.214`, inode 59781012, launched 30 seconds after the patch completed; the machine's one other live claude process, PID 3725, holds the same patched inode) — patched dogfooding. One disk-level footnote with a correction to the running narrative: the re-sign shrank the file 1,184 bytes again (factory CodeDirectory 15,051+7 hash slots, 481,989 bytes, plus a 9,046-byte Developer ID certificate chain → ad-hoc 15,051+2), which makes **five consecutive lean factory layouts** (2.1.208, 210, 211, 212, 214) — no heavy layout has appeared since 2.1.207, so the earlier "alternating layouts" reading no longer holds: lean is now simply the norm. This is the **twenty-fourth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208, 210, 211, 212) to leave VH1 unfixed.

- Recorded the **2.1.212** re-check — installed and patched 2026-07-17, ledger entry backfilled today. Claude Code updated 2.1.211 → 2.1.212, a consecutive version step, so the two-binary diff brackets no skipped build. The structural anchor finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature appears 7 times. The parser site is byte-for-byte identical to 2.1.211 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), the ninth consecutive build carrying those names. Build provenance: the factory binary grew 2,084,832 bytes (242,445,680 → 244,530,512) and the parser site drifted +984,913 bytes (214,159,296 → 215,144,209); the Bun banner reads `Bun v1.4.0` unchanged. Anthropic's 2.1.212 changelog (48 entries, mechanical `grep -c`) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser: the release is session-orchestration surface — `/fork` becoming a background-session copy (with `/subtask` taking the old in-session role), per-session WebSearch and subagent caps, MCP calls auto-backgrounding past 2 minutes — plus a plan-mode permission fix (file-modifying Bash commands now prompt) and a worktree symlink fix, all layers away from the parser. A strings diff (8,523 added / 7,458 removed) shows 141 charCode/codePoint/tokenizer-flavored lines in the added set — the same re-segmentation crop as ever on sampling (the ajv schema-codegen block, a PEM parser, PowerShell launcher strings, highlight.js grammars), none the VH1 site; the added set's themes match the changelog's own (fork/subtask, subagent caps, sandbox, plugin surface). The patcher applied with no script change on install day (`!l`→`!0`, one byte; factory `09ecba2ab2df…` → patched `95f78cf6ed4f…`, file mtimes putting the patch at ~2026-07-17T04:05Z; the state file has since been superseded by 2.1.214's patch). Honest backfill boundary: no `lsof` was captured on install day, so this entry claims no live-inode dogfooding for 2.1.212 — today's re-verification covers the on-disk patched binary (bug 0 / fix 1, structural anchor re-run today) and its still-valid ad-hoc signature (CodeDirectory 14,895+2; the factory layout was lean 14,895+7 plus a 9,047-byte Developer ID chain, re-sign shrink the usual 1,184 bytes), and today's `lsof` shows no process still holding 2.1.212 — it served its day and was retired by the 2.1.214 update. This is the **twenty-third** effective upstream release since 2.1.181 to leave VH1 unfixed.

## 2026-07-16

### Changed

- Bumped the tested badge to **2.1.211**. Claude Code updated 2.1.210 → 2.1.211 — a consecutive version step, so this two-binary diff brackets no skipped build (`~/.local/share/claude/versions/` now holds 208, 210, 211; 2.1.203 and 2.1.209 remain the only builds never installed on this machine). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.210 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), which now makes eight consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210, 2.1.211) carrying the same local names. Build provenance: the factory binary grew 935,712 bytes (241,509,968 → 242,445,680) and the parser site drifted +828,635 bytes (213,330,661 → 214,159,296); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.211 changelog (37 entries, mechanical `grep -c` over a direct fetch) has nothing on tool-call parsing, JSON tokenization, or the streaming parser. The entries that *sound* closest all sit in different layers: "Fixed permission previews relayed to chat channels not neutralizing bidirectional-override, zero-width, and look-alike quote characters, so tool inputs cannot visually alter the approval message" is display-layer anti-spoofing sanitization of the approval prompt, not the streaming tokenizer that parses tool arguments; "Improved background agent result reporting — Claude now … waits for the real completion instead of fabricating results" is a model-side confabulation guardrail (the same root-cause-B territory as 2.1.205's two guardrails), not a client parser fix; "Added `--forward-subagent-text` … to include subagent text and thinking in stream-json output" and the headless-stdin crash fix are CLI stream-json I/O, the same layer as 2.1.208/210; and "Updated integer environment variables … to accept scientific notation … like `1e6` and `64_000`" is env-var integer parsing, not the JSON string tokenizer. A strings diff (3,593 added / 2,958 removed) holds at zero on the VH1 core axis: no charCode/codePoint/tokenizer-flavored lines in the added set (continuing 2.1.210's zero), and the 10 generic `parse` hits all sit elsewhere — the same skill-prompt instruction ("the script will parse your output") and `/loop` interval-parsing template as 2.1.210, plus proxy-response parsing, ANSI `parseFloat`, a Storybook adapter, `parseInt` env handling, and a `parseToolPreset` helper; the `stream` hits are all CLI stream-json I/O, streaming-doc templates, and proxy/gateway code. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte — the ±260-byte window shows exactly one differing byte at the flag; factory `5a728a76198b…` → patched `0a96549030b8…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 36759, to `~/.local/share/claude/versions/2.1.211`, inode 59351799, the patched binary completed at 2026-07-16T01:25:28Z and this session launched 21 seconds later) — patched dogfooding. One disk-level footnote: the re-sign shrank the file only 1,184 bytes, matching the lean-CD pattern from 2.1.204/205/208/210 — the alternating factory signature layouts continue. This is the **twenty-second** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208, 210) to leave VH1 unfixed.

## 2026-07-15

### Changed

- Bumped the tested badge to **2.1.210**. Claude Code updated 2.1.208 → 2.1.210 — skipping 2.1.209 (which was never installed on this machine; `~/.local/share/claude/versions/` now holds 207, 208, 210; upstream's changelog for 2.1.209 is a single entry — a dialog-fix revert — so nothing to bracket), and the daily-update cadence resumed after the 2.1.207→208 weekend pause (2.1.208 Monday 2026-07-14, 2.1.210 Tuesday 2026-07-15). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.208 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), which now makes seven consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210) carrying the same local names. Build provenance: the factory binary grew 1,264,032 bytes (240,245,936 → 241,509,968) and the parser site drifted +1,142,615 bytes (212,188,046 → 213,330,661); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.210 changelog (33 entries — mechanical `grep -c`, not the 42 the summarization model hallucinated) has nothing on tool-call parsing, JSON tokenization, or the streaming parser. The entries that *sound* closest all sit in different layers: "Fixed paste markers leaking into external editors … which could appear as stray È/É characters around pasted text" is paste-clipboard encoding, not streaming token parsing; "Hardened the Agent tool against indirect prompt injection via content a subagent read" is a model-side security measure, not a parser fix; and "Fable temporarily shows as unavailable in the advisor picker while a server-side issue causing Fable advisor failures is fixed" is model availability, not parsing. A strings diff (4,193 added / 3,896 removed) returns to zero on the VH1 core axis: no charCode/codePoint/tokenizer-flavored lines in the added set (the 2.1.208 release's 5-hit blip did not persist), and the 3 generic `parse` hits all sit elsewhere — a skill-prompt instruction ("the script will parse your output"), a `/loop` interval-parsing prompt template, and a utility function; the 7 `stream` hits are all CLI stream-json I/O and proxy/gateway code. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `1b471d62d111…` → patched `2891b44543c7…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 27421, to `~/.local/share/claude/versions/2.1.210`, inode 59218077, the patched binary completed at 2026-07-15T00:53:33Z) — patched dogfooding. One disk-level footnote: the re-sign shrank the file only 1,184 bytes, matching the lean-CD pattern from 2.1.204/205/208 — the alternating factory signature layouts continue. This is the **twenty-first** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208) to leave VH1 unfixed.

## 2026-07-14

### Changed

- Bumped the tested badge to **2.1.208**. Claude Code updated 2.1.207 → 2.1.208 — again a consecutive version step, so the two-binary diff brackets no skipped build, though the four-day daily-update streak paused over the weekend (2.1.207 installed Friday 2026-07-11, 2.1.208 Monday 2026-07-14, with upstream's changelog showing no build in between); 2.1.203 remains the only build never installed (`~/.local/share/claude/versions/` now holds 206, 207, 208). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.207 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), which now makes six consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208) carrying the same local names. Build provenance: the factory binary shrank 991,200 bytes (241,237,136 → 240,245,936) — only the second shrink in this ledger after 2.1.204's, this time with no changelog line accounting for it (the release's many memory and disk reductions are runtime RAM and transcript disk, not binary size; at under 1 MB it sits within ordinary build-to-build variation) — and the parser site drifted +252,838 bytes (211,935,208 → 212,188,046); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.208 changelog (45 entries, the longest in this ledger) has nothing on tool-call parsing, JSON tokenization, or the streaming parser — the entries that *sound* adjacent all sit in the CLI's stream-json I/O protocol (the stdin/stdout JSON-lines framing for SDK hosts and `claude -p` piping), a different layer from the API-response tokenizer VH1 lives in: "truncated stream-json/JSON output … when piping large responses from `claude -p`" is print-mode stdout, "stream-json input killing the session on blank CRLF or whitespace-only lines" is SDK stdin framing, "headless stream-json sessions hanging … [on] a non-string `set_model` payload" is the SDK control protocol, and the Bedrock "Truncated event message received" fix renames a gateway error. One entry belongs in this repo's operational notes rather than the parser ledger: background-session attach failing permanently "after an update replaced the binary a running `claude agents` process was launched from" is upstream fixing a symptom of the same binary-replacement inode decoupling our macOS patch notes document — update plumbing, not parsing. A strings diff (6,132 added / 4,329 removed) breaks the two-release streak of zero tokenizer-flavored lines in the added set, but barely: 5 hits, each ruled out on inspection — a named-pipe `charCodeAt(0)` check, a Bun-internal `@String.fromCharCode` trace-event emitter, a TOML-style "Unterminated basic string" error-string table, a Rust-style `input[pos.cast()]` assertion, and a shell-layer "unterminated double quote" — none the character-level JSON string tokenizer; the 26 generic `parse` hits likewise sit elsewhere (a session-store importer skipping unparseable lines, `JSON.parse` over files read from disk, a lazy `parseUrl`, proxy-response and ANSI `parseFloat` code, ripgrep's own error-string table, Bun's bundler `ParseTask`). The added set matches the changelog's own themes — background/daemon/agent strings (96), the new `CLAUDE_CODE_PROCESS_WRAPPER` corporate-launcher surface (43), memory-leak/LRU work (18), transcript/checkpoint pruning (9) — none reaching the parser. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte — the ±260-byte window shows exactly one differing byte; factory `051c7f28871b…` → patched `98652a9956ea…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 66678, to `~/.local/share/claude/versions/2.1.208`, inode 59142150, launched 88 seconds after the patch completed at 2026-07-14T02:32:06Z) — patched dogfooding. One disk-level footnote, correcting 2.1.207's reading: the re-sign shrank the file only 1,184 bytes because the factory 2.1.208 CodeDirectory is back on the lean layout — 14,634+7 hash slots (468,645 bytes) plus the usual 9,046-byte Developer ID certificate chain, replaced by a 14,634+2-slot ad-hoc one; with 2.1.202/206/207 heavy and 2.1.204/205/208 lean, the two factory signature layouts now read as alternating rather than the lean ones being a deviation — a mechanical signing detail either way, with no content meaning. This is the **twentieth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207) to leave VH1 unfixed.

## 2026-07-11

### Changed

- Bumped the tested badge to **2.1.207**. Claude Code updated 2.1.206 → 2.1.207 — the fourth daily update in a row (2.1.204 on 2026-07-08, 2.1.205 on 2026-07-09, 2.1.206 on 2026-07-10, 2.1.207 on 2026-07-11), again a consecutive version step, so the two-binary diff brackets no skipped build; 2.1.203 remains the only build never installed (`~/.local/share/claude/versions/` now holds 205, 206, 207). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.206 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), which now makes five consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207) carrying the same local names. Build provenance: the factory binary grew 842,112 bytes (240,395,024 → 241,237,136) and the parser site drifted +733,820 bytes (211,201,388 → 211,935,208); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.207 changelog (25 entries) has nothing on tool-call parsing, JSON tokenization, or the streaming parser — several entries *sound* adjacent but sit elsewhere: "terminal freezing and keystrokes lagging during streaming of very long lists" is the render layer, "malformed bracket patterns in rules globs" is file-path glob handling, "crash loop in agent teams from malformed teammate mailbox message" is the agent-teams mailbox, the auto-updater no longer overwriting a custom launcher script or symlink at `~/.local/bin/claude` is update plumbing (this machine's launcher is the stock symlink, and the patch targets the versioned binary it resolves to, so nothing changes for this repo), and "spurious prompt-injection warnings from benign system-generated conversation updates" is a warning-classifier fix in the same *semantic* neighborhood as the cluster's model-side half but not the parser. A strings diff (3,267 added / 2,721 removed) stays as clean as 2.1.206's on the axis that matters: **zero** charCode/codePoint/tokenizer-flavored lines in the added set for the second release running, and the 6 generic `parse` hits all sit elsewhere (plugin shell-form warning copy, a script-output parsing note, a proxy-response parser, an ANSI-color `parseFloat`, a Storybook adapter comment, a schedule-interval parsing prompt). The added set matches the changelog's own themes — agent-flavored strings (37), plugin (24), auto-mode (22, the Bedrock/Vertex/Foundry opt-in change), teammate-mailbox (20), worktree (14), launcher/symlink (10) — none reaching the parser. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `1397a062c688…` → patched `da14ef36a630…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 4303, to `~/.local/share/claude/versions/2.1.207`, inode 58669478, launched 23 seconds after the patch completed at 2026-07-11T02:26:36Z) — patched dogfooding. One disk-level footnote: the re-sign shrank the file 1,403,616 bytes, the large-CD shape again — the factory 2.1.207 CodeDirectory carries 58,435+7 hash slots (1,870,277 bytes) plus a 9,046-byte Developer ID certificate chain, replaced by a 14,609+2-slot ad-hoc one (467,648 bytes); with 2.1.202, 206, and 207 all on the heavy factory signature, 2.1.204/205's lean ones now read as the deviation. This is the **nineteenth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206) to leave VH1 unfixed.

## 2026-07-10

### Changed

- Bumped the tested badge to **2.1.206**. Claude Code updated 2.1.205 → 2.1.206 — a separate update landing the morning after 2.1.205's (each install has its own dated entry: 2.1.204 on 2026-07-08, 2.1.205 on 2026-07-09, 2.1.206 on 2026-07-10 — the third daily update in a row), again a consecutive version step, so the two-binary diff brackets no skipped build; 2.1.203 remains the only build never installed (`~/.local/share/claude/versions/` now holds 204, 205, 206). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.205 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), which now makes four consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206) carrying the same local names. Build provenance: the factory binary grew 2,957,056 bytes (237,437,968 → 240,395,024) and the parser site drifted +1,153,245 bytes (210,048,143 → 211,201,388); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.206 changelog (27 entries) has nothing on tool-call parsing, JSON tokenization, or the streaming parser — it is CLI/UX and plumbing throughout (gateway `/login` endpoints, MCP `request_timeout_ms` honored from `--mcp-config`, `/model` picker row fixes, agents-view fixes, a Bedrock startup hang), nothing closer to the parser than that. A strings diff (4,548 added / 3,059 removed) is the cleanest yet on the axis that matters: **zero** charCode/codePoint/tokenizer-flavored lines in the added set (2.1.204 had two, 2.1.205 had ~125 rebuild-resegmentation hits), and the 13 generic `parse` hits all sit elsewhere (a chart runtime's `JSON.parse` calls in dataviz HTML templating, a backend wire-hint comment, a hosts-file regex note). The added set is scattered feature surface — CSS design tokens (70 `--cds-*` lines), chart/dataviz templating (47 hits), agent-flavored strings (47), plugin/LSP plumbing (~24), voice-mode copy (7) — none reaching the parser. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `3197aba4442d…` → patched `d60e80d290a4…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 47004, to `~/.local/share/claude/versions/2.1.206`, inode 58468534, launched three to four minutes after the patch completed at 2026-07-10T00:23:39Z) — patched dogfooding. One disk-level footnote: the re-sign shrank the file 1,398,720 bytes, back in 2.1.202 territory rather than 2.1.204/205's 1,184 — the 2.1.206 factory CodeDirectory returns to the large-slot layout (58,231+7 hash slots, 1,863,749 bytes, plus a 9,046-byte certificate signature) that the ad-hoc re-sign replaces with a 14,558+2-slot one; a mechanical consequence of re-signing, not a content change. This is the **eighteenth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205) to leave VH1 unfixed.

## 2026-07-09

### Changed

- Bumped the tested badge to **2.1.205**. Claude Code updated 2.1.204 → 2.1.205 — a separate update landing one day after 2.1.204's (each install has its own dated entry: 2.1.204 on 2026-07-08, 2.1.205 on 2026-07-09), and this time the version step is consecutive, so the two-binary diff brackets no skipped build; 2.1.203 remains the only build never installed (`~/.local/share/claude/versions/` now holds 202, 204, 205). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.204 — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`) — which now makes three consecutive builds (2.1.202, 2.1.204, 2.1.205) carrying the same local names, further settling that 2.1.202's reshuffle was a one-off rather than a new rename cadence. Build provenance: the factory binary grew back 476,064 bytes (236,961,904 → 237,437,968) and the parser site drifted +396,618 bytes (209,651,525 → 210,048,143); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.205 changelog (22 entries) has nothing on tool-call parsing, JSON tokenization, or the streaming parser: the closest are a `--json-schema` fix (structured-output *schema validation*, not the character-level JSON string tokenizer) and "Auto-update binary downloads now stream to disk … cutting the updater's peak memory usage by roughly 400 MB" — that 400 MB is the updater process's RAM, not the binary size (which grew), and is separate from 2.1.203's on-disk shrink. Two model-behavior guardrails (background-task notifications now stating "no human input has occurred" to stop *fabricated in-transcript approvals*, and an auto-mode rule blocking tampering with transcript files) speak to the model-side confabulation half of the bug cluster, not to the client parser VH1 lives in. A strings diff (4,966 added / 4,655 removed) is noisier than 2.1.204's clean two-hit set — ~125 charCode/codePoint/tokenizer-flavored lines in the added set — but sampling every one puts them all in pre-existing character-handling routines shifted by the rebuild (UTF-8 codec loops, digit scanners, third-party `marked`/PowerShell/JSON-number tokenizers), none the VH1 `push({type:"string",value:…})` site; the count is high only because `strings` re-segments minified library code once surrounding bytes move, and the verdict rests on the structural anchor's byte read of the parser-site window (identical to 2.1.204), never on the strings diff. The rest of the added set is new-feature surface: `workflow`/`schedule`/`cron` strings (~283 hits, matching this session's `Cron*`/`RemoteTrigger`/`Workflow` tools) and WebGL shader/blend code (~129 hits). The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `33e28624c5ae…` → patched `35a8d68cd2ca…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature (`codesign --verify`: "valid on disk … satisfies its Designated Requirement"), and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 66238, to `~/.local/share/claude/versions/2.1.205`, inode 58287616) — patched dogfooding. This is the **seventeenth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204) to leave VH1 unfixed.

## 2026-07-08

### Changed

- Bumped the tested badge to **2.1.204**. Claude Code updated 2.1.202 → 2.1.204 on this machine; 2.1.203 shipped upstream but was never installed locally (absent from `~/.local/share/claude/versions/`, which holds only 201, 202, 204), so this entry brackets it with a direct two-binary diff rather than chaining through it. The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.202 — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), and accumulator (`a`) — so the identifier reshuffle 2.1.202 introduced (the first break in the raw-frozen streak since 2.1.187) was a one-off rather than the start of a new rename cadence. Build provenance: the factory binary shrank 6.67 MB (243,631,376 → 236,961,904 bytes) and the parser site drifted −5,569,911 bytes (215,221,436 → 209,651,525). The shrink lines up with upstream's own account: 2.1.203's changelog (37 entries, mostly background-agent/daemon/worktree/session fixes) includes "Reduced binary size by ~7 MB and startup memory by ~7 MB by loading a large bundled dependency lazily instead of inlining it" — close enough to the measured 6.67 MB that no other explanation is needed. Neither 2.1.203's nor 2.1.204's changelog (a single entry: a SessionStart-hook streaming fix for headless sessions) mentions tool-call parsing or string tokenization. A strings diff (5,140 added / 3,094 removed) turns up only two charCode/codePoint/tokenizer-flavored hits in the added set, both tracing to unrelated pre-existing library code shifting position under the rebuild — a markdown renderer's own tokenizer (`this.tokenizer.code`, from `marked`) and a JS-grammar library's lexer error string (`Unable to Tokenize because Errors detected in definition of Lexer`, from `chevrotain`) — neither is the character-level JSON string tokenizer this repo patches. The bulk of the added strings sit in new feature surface instead: WebGL shader/blend code (~35 hits, a canvas rendering feature), the `chevrotain` parser-generator library itself (13 hits), and `workflow`/`schedule`/`routine`-flavored strings (19 combined) that match this session's own newly available `Cron*` and `RemoteTrigger` tools. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `1677b67595b6…` → patched `c5ca6692a3f5…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process to `~/.local/share/claude/versions/2.1.204`, inode 58124573, launched 22 seconds after the patch completed) — patched dogfooding. This is the **sixteenth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202) to leave VH1 unfixed.

## 2026-07-07

### Changed

- Bumped the tested badge to **2.1.202**. Claude Code updated 2.1.201 → 2.1.202 (consecutive). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times — but for the first time since 2.1.181, the window around the site is not raw-identical: 2.1.201's `,!l)n.push({type:"string",value:a})` (loop-char `r`, digit-test var `s`) becomes 2.1.202's `,!l)r.push({type:"string",value:a})` (loop-char `n`, digit-test var `i`) — the loop-char and receiver names swapped (`r`↔`n`) and the digit-test helper renamed (`s`→`i`), while the flag (`l`) and accumulator (`a`) held. Both builds' Bun runtime banner (`Bun v1.4.0`/`Bun/1.4.0`) is identical, so this reads as a local recompile shifting the minifier's naming, not a runtime/bundler upgrade like 2.1.181's — and the structural anchor, which matches the `type:"string"` AST tag rather than identifier names, was built for exactly this case. Build provenance: factory size grew 231,708,784 → 243,631,376 bytes (+11.4 MB, the largest single-version jump in this ledger) and the parser site drifted 207,862,449 → 215,221,436 (+7,358,987 bytes). A strings diff (5,819 added / 5,251 removed, also the largest yet) lines up with Anthropic's own 2.1.202 changelog: a new `/config` "Dynamic workflow size" setting, `workflow.run_id`/`workflow.name` OpenTelemetry attributes, and a `/workflows` layout pass — corroborated by added strings like `CORRECTNESS_ANGLES`, `finder_budget`, bundled "Managed Agents" reference docs, and a Storybook-source converter. One official line, "Fixed workflow scripts with unicode quote escapes in strings being corrupted before parsing," is worth naming so it isn't mistaken for a VH1-adjacent fix: that's an acorn-style JS source parser (`ecmaVersion:"latest",sourceType:"module"`, present unchanged in both builds, just shifted) validating user-authored workflow scripts — a different parser at a different layer than the character-level JSON tokenizer this repo patches. The ~122 charCode/codePoint/tokenizer-flavored hits in the strings diff trace to that same pre-existing library code shifting position under the rebuild, not new tokenizer logic. This is the **fifteenth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `7414f707861e…` → patched `a13d8253c64a…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (patched dogfooding). One disk-level footnote: the patched file is 1,417,536 bytes smaller than the factory original — not the content patch itself (a same-length 1-byte flip) but the re-sign: Apple's Developer ID CodeDirectory (1,888,837 bytes, 59,015+7 hash slots, plus a 9,047-byte certificate-chain signature) is replaced by a smaller ad-hoc one (472,288 bytes, 14,754+2 slots, no certificate blob), accounting for essentially all of it — a known, mechanical consequence of ad-hoc re-signing, not a content change.

## 2026-07-04

### Changed

- Bumped the tested badge to **2.1.201**, folding in two upstream releases at once — Claude Code updated 2.1.199 → 2.1.200 (the 2.1.200 bump landed overnight and was verified but not separately recorded) → 2.1.201. Both transitions leave VH1 unpatched upstream: the ±260-byte window around the parser site is byte-for-byte identical across 2.1.199, 2.1.200 and 2.1.201 — still `,!l)n.push({type:"string",value:a})`, no normalization needed — the ninth and tenth raw-frozen builds in a row (unbroken since 2.1.187). The structural anchor finds exactly one vulnerable site in each factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature appears the same 7 times in all three. Build provenance: 2.1.199 → 2.1.200 shrank 446,752 bytes (232,155,536 → 231,708,784) with the site drifting 799,552 bytes (207,062,833 → 207,862,385); 2.1.200 → 2.1.201 is the unusual case — both factory binaries are *exactly* 231,708,784 bytes, yet they are **not** byte-identical (first difference at offset 2112, a Mach-O load-command address field) and the parser site still drifted 64 bytes (207,862,385 → 207,862,449), confirming a genuinely new build that merely repacked to the same size. A strings diff over 2.1.200 → 2.1.201 shows 158 short strings added and 160 removed, with zero touching the character-level string tokenizer (no `charCode`/`codePoint`/`tokenizer` additions) — the added set is dominated by gateway/session/feature-flag surface (`allowedHttpHookUrls`, `disableRemoteControl`, `disableClaudeAiConnectors`, `/workflows`, Sessions-API labels), none reaching the parser. These are the **thirteenth and fourteenth** effective upstream releases since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199) to leave VH1 unfixed. The version-agnostic patcher re-applied to 2.1.201 with no script change (`!l`→`!0`, one byte; factory `a0852d76…` → patched `a9941c6f…`), verified on disk (bug 0 / fix 1) with a valid ad-hoc signature and by the running session's mmap'd inode (PIDs 45409/50688 both hold inode 57281235 — patched dogfooding).

## 2026-07-03

### Changed

- Documented the model-side boundary across README and the investigation doc, and posted a [second community comment on #62123](https://github.com/anthropics/claude-code/issues/62123#issuecomment-4878159880) contributing two weeks of forensics data: the stray-token pattern before leaked `<invoke>` XML (`câ`/`call`/`court`/`count` — all c-initial, all in the opener position, across CLI/Desktop and Chinese/Japanese/Korean-heavy sessions), and a confabulation shape verified the same day in which the model imagined tool runs inside a single thinking block and issued false "prompt injection" / "environment corrupted" alerts — every claim failing the claim-vs-artifact transcript check (related to the #64409 cluster). A new Technical Limitations entry makes the scope explicit: model-side shapes are upstream of any client fix; what the repo offers there is triage (fingerprinting which shape you're hitting), not defense.
- Bumped the tested badge to **2.1.199**. Claude Code updated 2.1.198 → 2.1.199 (consecutive). A two-way binary diff confirms VH1 remains unpatched upstream: the ±260-byte window around the parser site is byte-for-byte identical to 2.1.198 — still `,!l)n.push({type:"string",value:a})`, no normalization needed, the eighth raw-frozen build in a row (unbroken since 2.1.187). The structural anchor still finds exactly one vulnerable site in the whole binary (bug 1 / fix 0), and the character-scan loop signature `e[++t]` appears the same 7 times in both binaries. The factory binary grew 2.83 MB (229,328,464 → 232,155,536) and the site drifted 1,326,226 bytes (205,736,607 → 207,062,833), confirming a genuinely new build with the parser frozen in place. A strings diff puts all 4,031 added (and 2,708 removed) short strings elsewhere — roughly 1,400 are bundler-generated class-constructor guards (`Cannot call a class constructor _XX without |new|`, a toolchain-level change), and the rest is dominated by CSS design tokens (violet/magenta/neutral palettes, `--hl-*` highlight theme, Anthropic Sans/Mono) for a UI template layer — with zero new tokenizer strings and a single generic `parse` hit (`parseRepoSlug`), none touching the character-level string tokenizer. This is the **twelfth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `e3cb61ab…` → patched `c154554c…`), verified on disk (bug 0 / fix 1), by signature, and by the running session's mmap'd inode (this very session, launched 21 seconds after the patch completed — patched dogfooding).

## 2026-07-02

### Fixed

- **Hook: counter arithmetic broke on a counter file with no entry for the current session+tool.** `grep -c` prints `0` AND exits 1 on no match, so the old `|| echo 0` stacked a second zero onto the count (`0\n0`), producing shell `syntax error` noise in the block message and defeating the 3-strike CRITICAL escalation. Observed live on 2026-06-30 (the message read "Blocked calls in this session: 0\n0"). The count is now taken from `grep` alone with a numeric-format guard, and a regression test covers the dirty-counter-file case the old suite never exercised (it always started from a deleted counter file).

### Changed

- **Hook: MCP `{}` is now judged by per-session history instead of blocked unconditionally.** The only real-world firing of the old blanket rule was a false positive: a legitimately zero-argument MCP call (`tabs_context_mcp`, whose schema is all-optional) was blocked, and the model was told the arguments would "be empty again" and to `/clear` a healthy session. Hooks can't read MCP schemas, so a bare `{}` carries no signal by itself — but the VH1 poisoning signature does: a tool that sent real arguments earlier in the session, then collapsed to `{}`. The hook now records non-empty MCP calls per session (`~/.claude/state/evap-shield-nonempty`) and blocks `{}` only for a tool with that history; a first `{}` passes and is logged as `allowed`. The block message now leads with the history evidence, drops the unconditional "known bug" framing, and gains an escape hatch for intentionally-empty calls. Log lines gain an `action` field (`blocked`/`allowed`). Hook test suite: 25 → 30 tests (history-gate three-state, cross-session isolation, dirty counter, log actions).

- Bumped the tested badge to **2.1.198**. Claude Code updated 2.1.197 → 2.1.198 (consecutive). A two-way binary diff confirms VH1 remains unpatched upstream: the ±260-byte window around the parser site is byte-for-byte identical to 2.1.197 — still `,!l)n.push({type:"string",value:a})`, no normalization needed, the seventh raw-frozen build in a row (after 187→191, 191→193, 193→195, 195→196, 196→197, and now 197→198) and unbroken since 2.1.187. The structural anchor still finds exactly one vulnerable site in the whole binary (bug 1 / fix 0), and the character-scan loop signature `e[++t]` appears the same 7 times in both binaries. The factory binary grew 2.08 MB (227,251,472 → 229,328,464) and the site drifted 3,048,115 bytes (202,688,492 → 205,736,607), confirming a genuinely new build with the parser frozen in place. A strings diff puts all 15,452 added (and 3,719 removed) short strings elsewhere — the bulk traces to this release's two headline additions, a highlight.js 11 syntax-highlighting upgrade (large per-language keyword dictionaries) and the new `/dataviz` skill (color-palette validator, chart-design copy) — with zero new tokenizer strings and 31 generic `parse` hits (argparse, YAML, Storybook, proxy responses), none touching the character-level string tokenizer. This is the **eleventh** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196, 197) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `ab6f7ee1…` → patched `5b923d8e…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session, launched 19 seconds after the patch completed — patched dogfooding), and by launch timing.

## 2026-07-01

### Changed

- Bumped the tested badge to **2.1.197**. Claude Code updated 2.1.196 → 2.1.197 (consecutive). A two-way binary diff confirms VH1 remains unpatched upstream: the ±120-byte window around the parser site is byte-for-byte identical to 2.1.196 — still `,!l)n.push({type:"string",value:a})`, no normalization needed, the sixth raw-frozen build in a row (after 187→191, 191→193, 193→195, 195→196, and now 196→197) and unbroken since 2.1.187. The structural anchor still finds exactly one vulnerable site in the whole binary (bug 1 / fix 0). The factory binary grew 1.4 MB (225,782,608 → 227,251,472) and the site drifted 100,324 bytes (202,588,168 → 202,688,492), confirming a genuinely new build with the parser frozen in place. A strings diff puts all 3,526 added (and 3,368 removed) short strings elsewhere — tool/agent/context/task/model/MCP/auth subsystems — with tokenizer (+22) and parser (+64) counts minimal and none touching the character-level string tokenizer. This is the **tenth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195, 196) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `8cc0c4d1…` → patched `e94ede6d…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session — patched dogfooding), and by launch timing.

## 2026-06-30

### Changed

- Bumped the tested badge to **2.1.196**. Claude Code updated 2.1.195 → 2.1.196 (consecutive). A two-way binary diff confirms VH1 remains unpatched upstream: the ±120-byte window around the parser site is byte-for-byte identical to 2.1.195 — still `,!l)n.push({type:"string",value:a})`, no normalization needed, the fifth raw-frozen build in a row (after 187→191, 191→193, 193→195, and now 195→196) and unbroken since 2.1.187. The structural anchor still finds exactly one vulnerable site in the whole binary (bug 1 / fix 0). The factory binary grew 1.1 MB (224,682,640 → 225,782,608) and the site drifted 981,275 bytes (201,606,893 → 202,588,168), confirming a genuinely new build with the parser frozen in place. A strings diff puts all 10,262 added (and 7,616 removed) short strings elsewhere — agent/plugin/MCP/skill/workflow/sandbox/OAuth/Bedrock subsystems — none touching the character-level string tokenizer. This is the **ninth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193, 195) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `6fc6e61a…` → patched `7fed84d2…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session — patched dogfooding), and by launch timing.

## 2026-06-27

### Changed

- Bumped the tested badge to **2.1.195**. Claude Code updated 2.1.193 → 2.1.195 (2.1.194 skipped). A two-way binary diff confirms VH1 remains unpatched upstream: the ±120-byte window around the parser site is byte-for-byte identical to 2.1.193 — still `,!l)n.push({type:"string",value:a})`, no normalization needed, the third raw-frozen build in a row (after 187→191 and 191→193) and unbroken since 2.1.187 — and even the preceding escape-scan loop (`if(r==="\\"){…l=!0;break}a+=r+e[t]`) is untouched. The structural anchor still finds exactly one vulnerable site in the whole binary (bug 1 / fix 0). The factory binary grew 2.43 MB (222,248,240 → 224,682,640) and the site drifted 3,313,810 bytes (198,293,083 → 201,606,893), confirming a genuinely new build with the parser frozen in place. A strings diff puts all 4,746 added (and 2,534 removed) short strings elsewhere — an LLM gateway/proxy relay layer (re-emitting Anthropic-shaped `text/event-stream`, Bedrock's AWS binary event-stream, stripping the client's `Authorization`), a JWE/JWK/OAuth credential layer, the agent/workflow subsystem, voice streaming, sandboxing, and a Storybook adapter — none touching the character-level string tokenizer. This is the **eighth** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191, 193) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `8b45adad…` → patched `84c24c42…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session — patched dogfooding), and by launch timing.

## 2026-06-26

### Changed

- Bumped the tested badge to **2.1.193**, catching up from 2.1.187 — the 2.1.191 re-check below (2026-06-25) was recorded in the investigation ledger but the public badge wasn't bumped at the time, so this entry covers both 2.1.191 and 2.1.193. Claude Code updated 2.1.191 → 2.1.193 (2.1.192 skipped). A two-way binary diff confirms VH1 remains unpatched upstream: the ±120-byte window around the parser site is byte-for-byte identical to 2.1.191 — still `,!l)n.push({type:"string",value:a})`, no normalization needed, continuing the raw-frozen streak since 2.1.187 — and the structural anchor still finds exactly one vulnerable site in the whole binary (bug 1 / fix 0). The factory binary grew another 2.39 MB (219,856,224 → 222,248,240) and the site drifted 989,822 bytes (197,303,261 → 198,293,083), confirming a genuinely new build. A strings diff puts all 3,701 new short strings elsewhere — Bun runtime stream builtins (`@putByIdDirectPrivate(readableStreamController…)`, an HTTP "Parse Error" path), a workflow-VM sandbox (`attacker-reachable` clone walker), and a feedback-report UI template — none touching the character-level string tokenizer. This is the **seventh** effective upstream release since 2.1.181 (after 2.1.183, 185, 186, 187, 190, 191) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `f7513a30…` → patched `cadbe992…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session — patched dogfooding), and by launch timing.

## 2026-06-25

### Changed

- Re-checked 2.1.190 and 2.1.191. After 2.1.187, Claude Code shipped 2.1.190 (2026-06-24; 188/189 skipped) and 2.1.191. Both are genuinely new builds (factory size 215,994,048 → 217,273,568 → 219,856,224; parser site drifted to 197,303,261 in 191) and both leave the site frozen at `,!l)n.push({type:"string",value:a})`, raw-identical to 2.1.187. A strings diff attributes the growth to a Bun runtime upgrade (HTTP agent/proxy/tunnel, async_hooks) and the workflow/agent subsystem; 2.1.191's eighteen changelog entries (/rewind, background agents, sandboxing, MCP retry, a 37% CPU cut) touch none of the tool-call parser. 2.1.190 was a brief ~8-hour unpatched window — never patched before 191 superseded it — and 191 was re-patched and verified by byte, inode, and launch timing. These are the **fifth** and **sixth** effective upstream releases since 2.1.181 to leave VH1 unfixed. (The public badge bump for these was folded into the 2026-06-26 entry.)

## 2026-06-24

### Changed

- Bumped the tested badge to **2.1.187**. Claude Code updated 2.1.186 → 2.1.187 (consecutive again — the second back-to-back bump after 2.1.185→2.1.186). A two-way binary diff confirms VH1 remains unpatched upstream: the ±260-byte window around the parser site is byte-for-byte identical to 2.1.186 — still `,!l)n.push({type:"string",value:a})`, names unshuffled, identical without even needing normalization — and the structural anchor still finds exactly one vulnerable site in the whole binary (bug 1 / fix 0). Unlike the +858 KB 2.1.186 build, 2.1.187 *shrank*: original size 216811232 → 215994048 (−817,184 bytes), while the site still drifted 193217884 → 193618676 (+400,792), confirming a genuinely new build rather than a recompile-free copy. A strings diff shows 2.1.187 is a real build (not a frozen reissue), but its 39 added / 26 removed human-readable messages all land elsewhere: `/toggle-memory` renamed to `/pause-memory`, Fable 5 usage-credit copy normalized (dropping "for a limited time" / "Included in your plan limits"), sandbox/credential-protection fields, GitHub Actions setup, and MCP idle-timeout — none of it touching the JSON parser. This is the **fourth** effective upstream release since 2.1.181 (after 2.1.183, 2.1.185, and 2.1.186) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `a59a16ba…` → patched `df0eb868…`), verified on disk (bug 0 / fix 1), by signature, and against the running session's mmap'd patched binary.
- Pre-disclosure documentation pass. Fixed the broken `{owner}` placeholder in both READMEs' clone URL (→ `tznthou`, which previously failed on copy-paste). Refreshed the #62123 stats (54 → 57 comments; "zero staff response" re-verified against the issue's commenter associations as of 2026-06-24 — all 57 are non-staff). Extended the investigation's §7 version ledger to cover 2.1.185–187 and folded in the external #70196 live case (takepan, 2.1.186, later marked `duplicate` / `area:model`). Marked FIX-PLAN.md as a decision-history snapshot frozen at 2026-06-16, with current analysis pointed to the investigation. Corrected the update-detector test count in both READMEs (18 → 21).

## 2026-06-23

### Changed

- Bumped the tested badge to **2.1.186**. Claude Code updated 2.1.185 → 2.1.186 (consecutive this time — no skipped version, unlike 2.1.182/2.1.184). A two-way binary diff confirms VH1 remains unpatched upstream: the ~280-byte window around the parser site is byte-for-byte identical to 2.1.185 — still `,!l)n.push({type:"string",value:a})`, names unshuffled — and the structural anchor still finds exactly one vulnerable site in the whole binary. Unlike the frozen-size 2.1.183→2.1.185 reissues, 2.1.186 is a *substantial* new build: original size grew 215952608 → 216811232 (+858,624 bytes) and the site drifted 192250818 → 193217884 (+967,066). A strings diff attributes the growth to sandbox / egress agent-proxy / managed-agents (subagent) / MCP resource tools / plugin governance — none of it touching the JSON parser. This is the **third** effective upstream release since 2.1.181 (after 2.1.183 and 2.1.185) to leave VH1 unfixed, and the strongest signal yet: upstream shipped ~858 KB of real new code and still didn't touch the parser. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `463a79cc…` → patched `8b277719…`), verified on disk (bug 0 / fix 1) and at launch (2.1.186).

## 2026-06-22

### Changed

- Bumped the tested badge to **2.1.185**. After Claude Code updated 2.1.183 → 2.1.185 (2.1.184 was skipped, as 2.1.182 was before it), a two-way binary diff confirms VH1 remains unpatched upstream: the parser site is byte-for-byte identical to 2.1.183 — still `,!l)n.push({type:"string",value:a})`, names unshuffled (Bun 1.4 carried over) — though it has drifted 64 bytes within the file (192250754 → 192250818), confirming a genuinely new build with the parser site frozen rather than a recompile-free copy. Original size is unchanged (215952608) and 2.1.185's new strings are all sandbox / agent-proxy / cloud-sessions / oauth / MCP governance, none touching the JSON parser. This is the **second** effective upstream release since 2.1.181 (after 2.1.183) to leave VH1 unfixed. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `a280c23b…` → patched `69862459…`), re-verified on disk, in the recorded state, and against the running session's mmap'd binary.

## 2026-06-20

### Changed

- `docs/vh1-investigation.md`: flagged that the MCP-tool hook coverage rests on validation *ordering* and was verified only on 2.1.179 — not re-checked on 181/183 like the parser site; and documented that this re-check is now automated by the `SessionStart` hook (a stat-only fingerprint fast path gates the anchored scan).

## 2026-06-19

### Added

- **SessionStart update-detection hook** (`check-update.sh`): fingerprints the live binary on each session start (stat-only fast path, silent when unchanged) and, when Claude Code was updated — which overwrites the binary patch — reports whether the VH1 patch needs re-applying (`vulnerable` → re-run the patcher; `unknown` → possibly fixed upstream, verify; `patched`/unchanged → quiet). Detection and reporting only: it never patches and never edits files. Standalone single file, fail-open, covered by `test-check-update.sh` (21 tests).

### Changed

- `install.sh` now installs both hooks (PreToolUse + SessionStart) with the same idempotent, non-destructive settings merge; the installer suite expanded to 26 tests.
- Bumped the tested badge to **2.1.183**. After Claude Code updated 2.1.181 → 2.1.183 (2.1.182 was skipped), a three-way binary diff confirms the upstream parser is byte-for-byte identical to 2.1.181 once identifiers are normalized — this time the minifier didn't even reshuffle names (still `l/n/a`) — so VH1 remains unpatched upstream. None of 2.1.183's sixteen changelog entries touch tool-call parsing. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte).

## 2026-06-18

### Added

- VH1 bug investigation writeup (`docs/vh1-investigation.md`), including a 2.1.181 re-check that confirms the bug remains unpatched upstream.

### Changed

- The VH1 patcher is now **version-agnostic**: a structural anchor on the parser's invariant replaces the hardcoded minified variable pattern, so it survives bundler/minifier reshuffles across Claude Code versions — verified across the 2.1.181 Bun 1.4 identifier rename.
- README (EN/ZH): documented the version-agnostic patcher — the 1-byte structural patch (previously described as a 2-byte literal), the 45-test patcher suite, and the 2.1.181 Bun 1.4 note.

## 2026-06-17

### Added

- Patcher failure-path regression suite (`test-patch-vh1.sh`) and installer merge-safety suite (`test-install.sh`).

### Changed

- Reframed the hook's scope in the docs: the patch is the root fix; the hook closes the MCP-tool gap that Claude Code's built-in validation leaves open.

### Fixed

- The hook handles malformed input and a missing `jq` gracefully (fail-open) instead of crashing.
- Restore is now atomic via `rename()`, with hardened patch-state handling.

### Security

- Restore verifies the backup's SHA-256 before replacing the binary.

## 2026-06-16

### Added

- Initial release: a PreToolUse hook (`evap-shield.sh`) and a binary patch (`patch-vh1.sh`) for the Claude Code VH1 streaming parser bug, with a one-command installer (`install.sh`).

### Fixed

- Prevent a macOS brick when patching: the patched Mach-O is ad-hoc re-signed and launch-checked on an isolated temporary inode, so patching never overwrites the running binary's inode (which AMFI would SIGKILL on relaunch).
