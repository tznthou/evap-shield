# Investigating Claude Code's tool-argument evaporation bug: the VH1 parser, a two-byte patch, and what it doesn't fix

*A field report on root-causing, patching, and verifying the bug where tool calls silently lose their arguments, with an honest account of what the fix does and does not cover.*

*Time anchor: the core binary-level findings are as of Claude Code 2.1.179 (2026-06-17), re-checked on every build through 2.1.214 (§7). The methods below let you re-check any later build yourself.*

## TL;DR

- **The bug is real and unfixed upstream.** Tool calls intermittently arrive with their arguments collapsed to `{}`. Three open issues track it (#62123, #67765, #63583); none had a maintainer reply as of 2026-06-24.
- **Root cause (one sub-shape of it):** a client-side streaming partial-JSON parser drops a string token when a value is cut across a streaming chunk boundary, collapsing the tool input to `{}`. We reached this independently; @in4mer filed the same analysis first in #67765.
- **It is patchable on the client.** Because this sub-shape lives in the client parser, a single two-byte binary patch (`!Y`→`!0`) stops the token from being discarded. As far as we found, evap-shield is the only tool that patches the parser itself rather than cleaning up after the fact.
- **The patch is verified correct by a white-box unit test: 760 truncation cases, 0 regressions.** It compares the original and patched parser byte-for-byte at every streaming cut point.
- **Official builds through 2.1.214 have not fixed it.** A 178↔179 binary diff shows the parser byte unchanged; 2.1.181's Bun 1.4 upgrade reshuffled the minified names while leaving the parser logic identical; every build from 2.1.183 through 2.1.201 — 183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201 — is byte-for-byte identical at the parser site with no further normalization needed; 2.1.202 reshuffles two of the site's local names again (a second, smaller-scope rename — Bun stays at 1.4.0, so it's not another runtime upgrade); and 2.1.204 (2.1.203 was never installed on this machine) reverts to matching 2.1.202's names exactly, with 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210, 2.1.211, 2.1.212, and 2.1.214 holding those same names again (2.1.209 and 2.1.213 also skipped on this machine), so that reshuffle was a one-off, not a new cadence (§7). The changelog's "partial responses preserved" is a higher-layer graceful-degradation feature, not a parser fix.
- **Honest boundary:** the patch covers one corner of a larger bug cluster. A second camp attributes other failures in the same cluster to the model emitting malformed markup, which a client patch cannot touch. We could not reproduce one of @in4mer's four points on 2.1.179. And the parser's primary failure path is, by construction, not reachable from a server-side mock, so the unit test rather than an end-to-end test is what verifies the fix.

## 1. The symptom: a tool call with no arguments

While writing this investigation, the tool I was using produced a clean example of the failure family it describes. A step was marked `completed`, but the content that step was supposed to deliver never appeared. The shell of the action was there; the payload was gone. My first instinct was that I had just been bitten by the bug under study.

I was wrong, and how I found out is the point. A single screenshot, `API Error: 529 Overloaded`, showed the real cause: a server-side interruption that cut the response stream mid-flight, between one block and the next. Not the parser bug. Not the agent skipping a step. A third cause I had not even listed.

That matters because the cause I jumped to and the cause that actually happened produce the *same observable symptom*: an action that reports success while its payload silently disappears. Across this whole bug cluster that is the recurring trap. The symptoms converge, the root causes do not, and you cannot tell them apart without evidence. (The 529 interruption is exactly the connection-drop shape that Section 6 shows *bypasses* the parser bug rather than triggering it.)

It happened a second time as I was committing this very writeup: a tool call came back *malformed and could not be parsed*, which is the exact headline symptom of #62123. This time it really looked like the bug, except the retry succeeded immediately, whereas #62123's signature is that the retry *also* fails. Same symptom, opposite tell, and once again not the cause I was about to write about.

The bug this report is about looks like this: a model decides to call a tool, the call goes out, and its `input` arrives as `{}`. The model reached for a tool and came back empty-handed. The same root cause surfaces downstream as three distinct symptoms:

- `input: {}`: the tool_use block is present but the arguments are empty. This is the main case in #67765.
- the tool_use block disappears entirely: `stop_reason` is `tool_use` but no block follows. This matches the title of #63583 verbatim.
- a silent stall: empty text, with `stop_reason` not set to `tool_use`.

It is real and not rare: #62123 has 57 comments, and both #67765 and #63583 carry a `has repro` label. I won't claim a frequency beyond that. There is no hard data for one, and the bug is intermittent by nature.

## 2. Two camps, no upstream fix, and nobody patching it

The bug has two competing root-cause theories, and they are not really fighting over the same failure; each fits a different sub-shape of the cluster.

- **Camp A, client parser.** A streaming partial-JSON parser drops tokens when a value is split across chunks, collapsing the input to `{}`. This is @in4mer's position and ours. Because it lives in the client, it can be patched there.
- **Camp B, model-side.** The model emits malformed legacy markup (bare invoke tags missing their prefix); the client is only reporting what it received, not producing the error. This cannot be fixed client-side; it can only be recovered from or degraded around.

This is not a question of one camp being wrong. The two likely describe different routes into the same observable failure, and Section 8 returns to where they part ways. It also happens that the people who diagnose it as a client parser bug are the ones who go on to patch the binary.

Upstream, nothing has moved. All three issues are open with zero maintainer replies, and none was touched after 2.1.179 shipped on the same day. Even the triage labels are split: `area:model` on #62123 and #63583, `area:core` and `area:mcp` on #67765. I'll note the split and leave the reading of it open.

The tooling around the bug is real but aimed elsewhere. @in4mer has the sharpest analysis of the lot but no tool. `claude-code-unpoison` rewinds a poisoned session after the fact so you can resume cleanly. `cc-safe-setup` is a large hook safety kit that classifies incidents and helps recover. None of them touches the parser.

Which leaves the question this report answers: can the client side actually *fix* this, not just recover, for Camp A's sub-shape? Yes, with a two-byte patch. The rest of this report is the evidence for that claim and the boundary around it.

## 3. Root cause: the VH1 streaming parser

The tool input passes through a four-layer pipeline, `JSON.parse(kH1(vH1(bFH(VH1(H)))))` (minified names, version-specific). VH1 is the streaming tokenizer, and it is where the bug lives. When a string value is split across streaming chunks and the closing quote has not arrived yet, VH1 discards the in-flight string token instead of holding it. The cascade collapses, and the final input parses to `{}`.

@in4mer's #67765, which he titled "accumulator shear," lays out the same first three stages: the parser drop, a secondary `?? {}` fallback that turns the broken parse into an empty object, and a per-tool cache that makes the empty result stick. He shipped a repro with it. We arrived at the same three stages independently, from our own extraction of the binary.

The fix, in concept, is to make the parser keep the incomplete string token rather than discard it, so the cascade never breaks. In the binary that is a two-byte change of the same length, with no offset shift. I'm deliberately not walking through the patching procedure here. The mechanism is the point of this section, and the tool in the repo carries its own safety rails for anyone who wants the rest.

## 4. Does the patch actually work? 760 tests, 0 regressions

The verification is white-box. Extract the four-layer pipeline, run the original and patched parser side by side, and compare their output byte-for-byte at every streaming truncation boundary.

**760 tests, 0 regressions.** Zero-cost, deterministic, no real API calls. The patch changes behaviour only in the truncated-string intermediate state; on the happy path the two parsers are byte-identical. That last part is what makes the change safe to ship: it stays inert unless the exact failure condition occurs.

Why a *unit* test is the verification that counts, rather than something end-to-end, is the subject of Section 6. Hold the thought.

## 5. Is it safe to patch a running binary?

The scare came first: a freshly patched binary exited 137 (SIGKILL) on launch. The obvious hypothesis was that the byte patch broke the Developer ID signature and AMFI rejected it.

A control experiment falsified that. The factory binary copied to a fresh path ran fine (exit 0). The *same bytes* written in place over the running inode exited 137, regardless of whether the signature was valid. The real cause is overwriting an inode that is currently mmap'd and executing: the on-disk code pages diverge from the running image, and AMFI SIGKILLs the next exec of that inode. It also explains why the patch only takes effect after a full restart.

The fix has three parts, and dropping any one re-bricks the binary:

1. **Ad-hoc re-sign after patching.** The byte change invalidates the Developer ID signature. With no Anthropic private key, an ad-hoc signature is the only local option, and it is deterministic (same input, same hash).
2. **Smoke-test a copy on a fresh path**, never the live inode. Otherwise the test re-triggers the 137 and the patch can never install.
3. **Size-check against the pre-resign size**, because the ad-hoc re-sign changes the binary's size.

On 2.1.179, live: patched, runs (exit 0, no brick), and rolls back cleanly. The binary shrinks by 1.28 MB in the process (226,082,208 → 224,766,816 bytes). That is the factory signature section being swapped for a smaller ad-hoc one, not the patch; the parser change itself is two same-length bytes.

The honest cost: a native update overwrites the patch. Every `claude update` returns the binary to its unpatched state, and the patch has to be re-applied. "A full restart" can itself be subtle: if a launcher or wrapper keeps the binary resident, switching windows or sessions may not release the running inode — check with `lsof <binary>` that nothing still holds it, both before patching and before expecting the patch to take effect. You are modifying a signed, vendored binary on your own machine; there is a backup, there are safety checks, and it is still at your own risk. This is defensive research on locally-installed software, not an invitation to go modifying binaries casually.

## 6. What we could not prove, and why

This section earns the rest of the report. Three honest limits.

**The parser's primary failure path is not reachable from a server-side mock.** We built a zero-cost end-to-end harness, a local mock server feeding truncated streams to the real binary, to watch the bug end to end. It could not trigger the primary path. Three different constructions all bypassed it: dropping the connection mid-stream made the transport retry instead of finalizing (the same connection-drop shape as the 529 in Section 1); an external interrupt aborted the whole operation; and a clean truncation fell through to the *secondary* `?? {}` fallback, not the primary parser drop. The primary path requires the client to actively commit a mid-stream partial buffer, something only the interactive TUI's abort-and-finalize handler does. So an end-to-end test *cannot* verify this patch; the unit test from Section 4 is the only thing that can. That is a structural boundary of the harness, not a missing tool.

**The hook layer guards less than it looks.** evap-shield ships a PreToolUse hook alongside the patch. Tested end to end: for built-in tools, an empty `{}` input is caught by Claude Code's own `InputValidationError` before the hook is ever called, so the hook is redundant there. For MCP tools it is different, and this is the one gap we have closed since the earlier writeup: we have now verified, on 2.1.179, that an MCP tool's `{}` input *does* reach the hook and evap-shield blocks it. (MCP tool schemas live server-side, so the client's `InputValidationError` does not fire first.) This rests on the client's validation *ordering* — MCP inputs are validated after the hook, built-ins before — which any build could change; unlike the parser site (Section 7), we checked it once on 2.1.179 and have not re-verified it on 181/183. There is a third shape: the tool_use block vanishes after the call already executed. The PreToolUse hook cannot catch that at all, because it runs before execution. The binary patch, not the hook, is the layer that covers the parser at its source.

**We could not reproduce one of @in4mer's four points.** His #67765 lists a fourth item: a top-level backslash handler that consumes two characters and drops the next `{` / `"` / `,`. Pulling the tokenizer out of the 2.1.179 binary and running it on real input, a top-level `\` followed by any of those tokenizes with nothing dropped. The handler skips only a single stray character. I want to be precise about what this is: *we could not reproduce his fourth point on 2.1.179*, which is not a refutation. He analyzed 2.1.173; the code may have changed between versions, or we may be looking at a different path than the one he meant. His first three points we reached independently and agree with. The shape we can reliably reproduce is the string-handler drop, and that is the one the patch targets.

## 7. Did the official 2.1.179 fix it?

The method matters more than the verdict, because the method outlasts any single version. The binary is a bun-compiled Mach-O with the JS bundle minified onto enormous single lines, so you can't diff a beautified file. Two anchored diffs do the job: (1) find the parser's `push` call, pull the bytes around it, and check whether the preceding bytes read `,!Y)` (original) or `,!0)` (patched); (2) extract short strings (`strings -a -n 6`, drop the long lines, sort-unique) and `comm` the two versions to surface what the newer build added.

The verdict, 178↔179: the parser byte is unchanged, and 179 still ships the original `,!Y)`. What 179 *did* add is all higher-layer: a "finalizing partial response" path, a partial-finalized telemetry event, and a friendly "connection closed mid-response" message, all operating at the SSE-block level rather than the JSON-token level. The changelog line "mid-stream connection drops: partial responses preserved" is graceful degradation, not a parser fix.

One counterintuitive consequence: now that the client preserves a partial response and feeds it onward, that incomplete JSON still has to pass through the unpatched parser. If anything, that could make the parser bug *easier* to hit on the connection-drop path, not harder. The official change moved the ball closer to the bug without fixing it.

Time anchor again: this is 2.1.179 on 2026-06-17. Re-run the two diffs on any later build to check it for yourself.

**Update, 2.1.181 (2026-06-18).** I re-ran both diffs on the next build (2.1.180 was skipped). The verdict holds — the parser is still unpatched — and the *way* it held is the point. A literal search for the old `,!Y)q.push` now finds nothing, yet the parser logic is structurally identical; only the names changed. The cause is in the changelog: the bundled Bun runtime was upgraded to 1.4, and the new bundler reshuffled the entire minified identifier space. The string handler's variables went from `Y/q/$` (2.1.179) to `l/n/a` (2.1.181), and the factory binary dropped from 226,082,208 to 215,193,056 bytes (~11 MB) — a new minifier, not a parser change. This is the section's thesis made concrete: a fixed byte-pattern rots across builds, so the durable anchor is the *structure* — `,!<flag>)<recv>.push({type:"string",value:<acc>})` — not the literal bytes. The same two-byte fix still applies; only the way you locate it must be version-agnostic.

**Update, 2.1.183 (2026-06-19).** Two builds on (2.1.182 was skipped), the verdict still holds — and this time the easy way. With Bun already at 1.4 since 2.1.181, the minifier reshuffled nothing: the string handler's variables are still `l/n/a`, and once identifiers are normalized the parser site is byte-for-byte identical to 2.1.181 — the same `,!l)n.push({type:"string",value:a})`. None of 2.1.183's sixteen changelog entries touch tool-call parsing (the nearest, "re-prompt once when a turn returns only a thinking block," is about *no output*, not *evaporated arguments* — a different failure). The structural anchor matched `!l`→`!0` on the first try, no script change. Across three consecutive builds — 2.1.179, 181, 183 — the parser is unpatched upstream.

**Update, 2.1.185 (2026-06-22).** (2.1.184 skipped.) Same verdict, same `,!l)n.push({type:"string",value:a})` with identifiers unchanged — but the parser site has drifted 64 bytes down the file (192,250,754 → 192,250,818), so this is a genuinely new build with the parser frozen in place, not a recompiled copy. The 78 new short strings are all sandboxing, an agent-proxy, cloud sessions, OAuth and MCP governance; none touch the JSON parser.

**Update, 2.1.186 (2026-06-23).** The first back-to-back version number (185→186, breaking the 182/184 skip pattern), and the strongest signal so far. Unlike the frozen-size rebuilds before it, 186 is a substantial build: the factory binary grew 858,624 bytes and the parser site moved almost a megabyte (to 193,217,884). That new code is all sandboxing, an egress agent-proxy, managed-agents, MCP resource tools, plugin governance — even a prompt for a "security monitor for autonomous AI coding agents." Anthropic shipped ~858 KB of real new code and still did not touch the parser. The same day, a third party independently filed #70196 (takepan, 2.1.186, macOS/iTerm) with the textbook symptom — "could not be parsed (retry also failed)," the error array itself evaporated to `[]` — confirming 186 is still affected from the outside. It was later marked `duplicate` of the main thread and tagged `area:model`, the same triage split noted in Section 2.

**Update, 2.1.187 (2026-06-24).** The second back-to-back number (186→187). This time the factory binary shrank 817,184 bytes, yet the ±260-byte window around the parser site matches raw — byte-for-byte, without even normalizing identifiers. The build is real, not a reshuffle: 39 strings added and 26 removed — a `/toggle-memory`→`/pause-memory` rename, usage-credit billing copy, sandbox and credential fields, GitHub Actions, an MCP idle timeout — none in the parser. That makes four effective builds since 2.1.181 (183, 185, 186, 187), none of them a fix, across two weeks of near-daily releases.

**Update, 2.1.191 (2026-06-25).** Two more builds, both unfixed. After 2.1.187, Anthropic shipped 2.1.190 (2026-06-24; 188 and 189 were skipped) and then 2.1.191. The parser site stays frozen at `,!l)n.push({type:"string",value:a})`, raw-identical to 2.1.187 across the window. The factory binary grew 215,994,048 → 217,273,568 (190) → 219,856,224 (191), and the site drifted to 197,303,261 in 191. A strings diff attributes the growth to a Bun runtime upgrade (HTTP agent/proxy/tunnel, async_hooks) and the workflow/agent subsystem; 2.1.191's eighteen changelog entries (/rewind, background agents, sandboxing, MCP retry, a 37% CPU cut) touch none of the tool-call parser. 2.1.190 was a brief ~8-hour unpatched window — never patched before 191 superseded it — and 191 was re-patched and verified by byte, inode, and launch timing. These are the fifth and sixth effective builds since 2.1.181 to leave VH1 unfixed.

**Update, 2.1.193 (2026-06-26).** The seventh. Claude Code updated 2.1.191 → 2.1.193 (2.1.192 skipped). The factory binary grew another 2.39 MB (219,856,224 → 222,248,240) and the parser site drifted 989,822 bytes (197,303,261 → 198,293,083), yet the ±120-byte window around it is byte-for-byte identical to 2.1.191 with no normalization needed — the raw-frozen streak now runs unbroken from 2.1.187. The structural anchor still finds exactly one vulnerable site in the whole binary. A strings diff puts all 3,701 new short strings elsewhere: Bun runtime stream builtins (`@putByIdDirectPrivate(readableStreamController…)`, an HTTP "Parse Error" path), a workflow-VM sandbox (an `attacker-reachable` clone walker with a propagation invariant), and a feedback-report UI template — none of it the character-level string tokenizer. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `f7513a30…` → patched `cadbe992…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session), and by launch timing — patched dogfooding. That is seven effective builds since 2.1.181 (183, 185, 186, 187, 190, 191, 193), none of them a fix.

**Update, 2.1.195 (2026-06-27).** The eighth. Claude Code updated 2.1.193 → 2.1.195 (2.1.194 skipped). The factory binary grew 2.43 MB (222,248,240 → 224,682,640) and the parser site drifted 3,313,810 bytes (198,293,083 → 201,606,893), yet the ±120-byte window around it is byte-for-byte identical to 2.1.193 with no normalization needed — even the preceding escape-scan loop is untouched, and the raw-frozen streak now runs unbroken from 2.1.187 across four releases. The structural anchor still finds exactly one vulnerable site in the whole binary. A strings diff puts all 4,746 new short strings elsewhere: an LLM gateway/proxy relay (re-emitting Anthropic-shaped `text/event-stream`, Bedrock's AWS binary event-stream, stripping the client's `Authorization`), a JWE/JWK/OAuth credential layer, the agent/workflow subsystem, voice streaming, sandboxing, and a Storybook adapter — none of it the character-level string tokenizer, the same theme as 187→191→193. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `8b45adad…` → patched `84c24c42…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session), and by launch timing — patched dogfooding. That is eight effective builds since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195), none of them a fix.

**Update, 2.1.196 (2026-06-30).** The ninth. Claude Code updated 2.1.195 → 2.1.196 (consecutive). The factory binary grew 1.1 MB (224,682,640 → 225,782,608) and the parser site drifted 981,275 bytes (201,606,893 → 202,588,168), yet the ±120-byte window around it is byte-for-byte identical to 2.1.195 with no normalization needed — the fifth consecutive raw-frozen build, unbroken from 2.1.187. The structural anchor still finds exactly one vulnerable site in the whole binary. A strings diff puts all 10,262 added (and 7,616 removed) short strings elsewhere: agent/plugin/MCP/skill/workflow/sandbox/OAuth/Bedrock subsystems — none of it the character-level string tokenizer. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `6fc6e61a…` → patched `7fed84d2…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session), and by launch timing — patched dogfooding. That is nine effective builds since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196), none of them a fix.

**Update, 2.1.197 (2026-07-01).** The tenth. Claude Code updated 2.1.196 → 2.1.197 (consecutive). The factory binary grew 1.4 MB (225,782,608 → 227,251,472) and the parser site drifted 100,324 bytes (202,588,168 → 202,688,492), yet the ±120-byte window around it is byte-for-byte identical to 2.1.196 with no normalization needed — the sixth consecutive raw-frozen build, unbroken from 2.1.187. The structural anchor still finds exactly one vulnerable site in the whole binary. A strings diff puts all 3,526 added (and 3,368 removed) short strings elsewhere — tool/agent/context/task/model/MCP/auth subsystems — with tokenizer (+22) and parser (+64) counts minimal and none touching the character-level string tokenizer. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `8cc0c4d1…` → patched `e94ede6d…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session), and by launch timing — patched dogfooding. That is the tenth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197), none of them a fix.

**Update, 2.1.198 (2026-07-02).** The eleventh. Claude Code updated 2.1.197 → 2.1.198 (consecutive). The factory binary grew 2.08 MB (227,251,472 → 229,328,464) and the parser site drifted 3,048,115 bytes (202,688,492 → 205,736,607), yet the ±260-byte window around it is byte-for-byte identical to 2.1.197 with no normalization needed — the seventh consecutive raw-frozen build, unbroken from 2.1.187 — and the `e[++t]` character-scan loop signature still appears the same 7 times. The structural anchor still finds exactly one vulnerable site in the whole binary. A strings diff puts all 15,452 added (and 3,719 removed) short strings elsewhere: the bulk traces to a highlight.js 11 syntax-highlighting upgrade (large per-language keyword dictionaries) and the new `/dataviz` skill (a color-palette validator, chart-design copy), with zero new tokenizer strings and 31 generic `parse` hits (argparse, YAML, Storybook, proxy responses), none touching the character-level string tokenizer. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `ab6f7ee1…` → patched `5b923d8e…`), verified on disk (bug 0 / fix 1), by signature, by the running session's mmap'd inode (this very session, launched 19 seconds after the patch completed), and by launch timing — patched dogfooding. That is the eleventh effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198), none of them a fix.

**Update, 2.1.199 (2026-07-03).** The twelfth. Claude Code updated 2.1.198 → 2.1.199 (consecutive). The factory binary grew 2.83 MB (229,328,464 → 232,155,536) and the parser site drifted 1,326,226 bytes (205,736,607 → 207,062,833), yet the ±260-byte window around it is byte-for-byte identical to 2.1.198 with no normalization needed — the eighth consecutive raw-frozen build, unbroken from 2.1.187 — and the `e[++t]` signature appears the same 7 times in both. The structural anchor still finds exactly one vulnerable site in the whole binary. A strings diff puts all 4,031 added (and 2,708 removed) short strings elsewhere: roughly 1,400 are bundler-generated class-constructor guards (`Cannot call a class constructor _XX without |new|`, a toolchain-level change) and the rest is dominated by CSS design tokens (violet/magenta/neutral palettes, `--hl-*` highlight theme, Anthropic Sans/Mono) for a UI template layer, with zero new tokenizer strings and a single generic `parse` hit (`parseRepoSlug`), none touching the character-level string tokenizer. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; original `e3cb61ab…` → patched `c154554c…`), verified on disk (bug 0 / fix 1), by signature, and by the running session's mmap'd inode (this very session, launched 21 seconds after the patch completed) — patched dogfooding. That is the twelfth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199), none of them a fix.

**Update, 2.1.200/2.1.201 (2026-07-04).** The thirteenth and fourteenth. Claude Code updated 2.1.199 → 2.1.200 (the bump landed overnight, verified but not separately recorded at the time) → 2.1.201. Both transitions leave the parser unpatched. 2.1.199 → 2.1.200 shrank 446,752 bytes (232,155,536 → 231,708,784) with the site drifting 799,552 bytes (207,062,833 → 207,862,385). 2.1.200 → 2.1.201 is the unusual case: both factory binaries are *exactly* 231,708,784 bytes, yet they are not byte-identical (first difference at offset 2112, a Mach-O load-command address field), and the parser site still drifted 64 bytes (207,862,385 → 207,862,449) — confirming a genuinely new build that merely repacked to the same size, not a frozen reissue. The ±260-byte window around the site is byte-for-byte identical across all three versions — still `,!l)n.push({type:"string",value:a})`, no normalization needed — and the structural anchor finds exactly one vulnerable site in each factory binary, with the `e[++t]` signature appearing the same 7 times in all three. A strings diff over 2.1.200 → 2.1.201 shows 158 added and 160 removed, with zero touching the character-level string tokenizer — the added set is dominated by gateway/session/feature-flag surface (`allowedHttpHookUrls`, `disableRemoteControl`, `disableClaudeAiConnectors`, `/workflows`, Sessions-API labels), none reaching the parser. The version-agnostic patcher re-applied to 2.1.201 with no script change (`!l`→`!0`, one byte; factory `a0852d76…` → patched `a9941c6f…`), verified on disk (bug 0 / fix 1) with a valid ad-hoc signature and by the running session's mmap'd inode (PIDs 45409/50688 both hold inode 57281235) — patched dogfooding. That is the thirteenth and fourteenth effective builds since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201), none of them a fix.

**Update, 2.1.202 (2026-07-07).** The fifteenth — and the first break in the raw-frozen streak since 2.1.181. Claude Code updated 2.1.201 → 2.1.202 (consecutive). The structural anchor still finds exactly one vulnerable site in the factory binary, and the `e[++t]` signature still appears 7 times, but the window around the site is no longer raw-identical: 2.1.201's `,!l)n.push({type:"string",value:a})` (loop-char `r`, digit-test var `s`) becomes 2.1.202's `,!l)r.push({type:"string",value:a})` (loop-char `n`, digit-test var `i`) — the loop-char and receiver names swapped (`r`↔`n`) and the digit-test helper renamed (`s`→`i`), while the flag (`l`) and accumulator (`a`) held. Unlike 2.1.181, this isn't a runtime upgrade — both builds' Bun banner reads `Bun v1.4.0` unchanged — so it reads as a local recompile nudging the minifier's naming, not a bundler-wide reshuffle; the structural anchor was built to survive exactly this (it matches the `type:"string"` AST tag, never the identifier names), and it did. The factory binary grew 11.9 MB (231,708,784 → 243,631,376), the largest single-version jump in this ledger, and the parser site drifted 7,358,987 bytes (207,862,449 → 215,221,436). A strings diff (5,819 added / 5,251 removed, also the largest yet) lines up with Anthropic's own 2.1.202 changelog: a new `/config` "Dynamic workflow size" setting and `workflow.run_id`/`workflow.name` OpenTelemetry attributes for the new multi-agent Workflow tool, corroborated by added strings like `CORRECTNESS_ANGLES`, `finder_budget`, and bundled "Managed Agents" reference docs. One official changelog line — "Fixed workflow scripts with unicode quote escapes in strings being corrupted before parsing" — names a real parser fix, but a different one: it's an acorn-style JS source parser (`ecmaVersion:"latest",sourceType:"module"`, present unchanged in both builds, just shifted) validating user-authored workflow scripts, not the character-level JSON tokenizer this section tracks. The ~122 charCode/codePoint/tokenizer-flavored hits in the diff trace to that same pre-existing library code shifting position under the rebuild. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `7414f707861e…` → patched `a13d8253c64a…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode — patched dogfooding. (One footnote for anyone diffing the patched file itself: it is 1,417,536 bytes smaller than the factory original — not the content patch, which is a same-length 1-byte flip, but the re-sign. Apple's Developer ID CodeDirectory, 1,888,837 bytes over 59,015+7 hash slots plus a 9,047-byte certificate-chain signature, is replaced by a smaller ad-hoc CodeDirectory, 472,288 bytes over 14,754+2 slots with no certificate blob — a known, mechanical consequence of ad-hoc re-signing, not a content change.) That is the fifteenth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202), none of them a fix.

**Update, 2.1.204 (2026-07-08).** The sixteenth. Claude Code updated 2.1.202 → 2.1.204 on this machine; 2.1.203 shipped upstream but was never installed locally (absent from `~/.local/share/claude/versions/`, which holds only 201, 202, 204), so this entry brackets it with a direct two-binary diff rather than chaining through it. The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.202 — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), and accumulator (`a`) — so the identifier reshuffle 2.1.202 introduced (the first break in the raw-frozen streak since 2.1.187) was a one-off, not the start of a new rename cadence: whatever changed between 2.1.201 and 2.1.202 held steady across this jump. Build provenance: the factory binary shrank 6.67 MB (243,631,376 → 236,961,904 bytes) and the parser site drifted −5,569,911 bytes (215,221,436 → 209,651,525). The shrink lines up with upstream's own account: 2.1.203's changelog (37 entries, mostly background-agent/daemon/worktree/session fixes) includes "Reduced binary size by ~7 MB and startup memory by ~7 MB by loading a large bundled dependency lazily instead of inlining it" — close enough to the measured 6.67 MB that no other explanation is needed. Neither 2.1.203's nor 2.1.204's changelog (a single entry: a SessionStart-hook streaming fix for headless sessions) mentions tool-call parsing or string tokenization. A strings diff (5,140 added / 3,094 removed) turns up only two charCode/codePoint/tokenizer-flavored hits in the added set, and both trace to unrelated pre-existing library code shifting position under the rebuild — a markdown renderer's own tokenizer (`this.tokenizer.code`, from `marked`) and a JS-grammar library's lexer error string (`Unable to Tokenize because Errors detected in definition of Lexer`, from `chevrotain`) — neither is the character-level JSON string tokenizer this section tracks. The bulk of the added strings sit in new feature surface instead: WebGL shader/blend code (~35 hits, a canvas rendering feature), the `chevrotain` parser-generator library itself (13 hits), and `workflow`/`schedule`/`routine`-flavored strings (19 combined) that match this session's own newly available `Cron*` and `RemoteTrigger` tools. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `1677b67595b6…` → patched `c5ca6692a3f5…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process to `~/.local/share/claude/versions/2.1.204`, inode 58124573, launched 22 seconds after the patch completed) — patched dogfooding. (One disk-level footnote, smaller this time: the patched file is only 1,184 bytes smaller than the factory original, not the ~1.4 MB seen at 2.1.202. The factory 2.1.204 CodeDirectory already carries far fewer, coarser-grained hash slots than prior versions — 14,434+7 versus the ~59,015+7 scale seen through 2.1.202 for a similarly sized binary — alongside the usual 9,047-byte Developer ID certificate chain; our ad-hoc CodeDirectory (14,434+2 slots, no certificate) removes the same certificate chain but far fewer hash-slot bytes than before, since this factory build's own signature was already leaner.) That is the sixteenth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204), none of them a fix.

**Update, 2.1.205 (2026-07-09).** The seventeenth. Claude Code updated 2.1.204 → 2.1.205 — a separate update landing one day after 2.1.204's, and this time a consecutive version step, so this two-binary diff brackets no skipped build; 2.1.203 remains the only gap and is still absent (`~/.local/share/claude/versions/` now holds 202, 204, 205). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.204 — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), and accumulator (`a`) — which now makes three consecutive builds (2.1.202, 2.1.204, 2.1.205) carrying the same local names, further settling that 2.1.202's reshuffle was a one-off rather than a new rename cadence. Build provenance: the factory binary grew back 476,064 bytes (236,961,904 → 237,437,968), a small climb that undoes about a fourteenth of 2.1.203's ~7 MB lazy-load shrink, and the parser site drifted +396,618 bytes (209,651,525 → 210,048,143); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade is in play. Anthropic's own 2.1.205 changelog (a large release — 22 entries plus a VSCode note) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser: the closest are a `--json-schema` fix (structured-output *schema validation*, not the character-level JSON string tokenizer) and "Auto-update binary downloads now stream to disk instead of buffering in memory, cutting the updater's peak memory usage by roughly 400 MB" — that 400 MB is the *updater process's* peak RAM, not the binary's size (which, as measured, actually grew), so it is unrelated to both this parser and to 2.1.203's separate on-disk shrink. Two other entries — background-task notifications now stating "no human input has occurred" to stop *fabricated in-transcript approvals*, and an auto-mode rule blocking tampering with session transcript files — are model-behavior guardrails that speak to the *other* half of this bug cluster (the model-side confabulation camp of §8), not to the client parser VH1 lives in. A strings diff (4,966 added / 4,655 removed) is noisier than 2.1.204's clean two-hit set: it turns up ~125 charCode/codePoint/tokenizer-flavored lines in the added set, but sampling them one by one puts every last one in pre-existing character-handling routines shifted into new positions by the rebuild — UTF-8 codec loops (`String.fromCharCode`, surrogate-pair `charCodeAt`), digit scanners (`r>=48&&r<=57`), and third-party tokenizers (a `marked`-style lexer, a PowerShell string handler, a JSON-number scanner) — none of them the VH1 `,!<flag>)…push({type:"string",value:…})` site. The count is high only because `strings` re-segments minified single-line library code differently once surrounding bytes move, so whole unchanged spans re-appear as "added"; the verdict never rests on the strings diff anyway — it rests on the structural anchor's byte-for-byte read of the parser-site window (identical to 2.1.204). The rest of the added set is the usual new-feature surface: `workflow`/`schedule`/`routine`/`cron`-flavored strings (~283 hits, matching this session's own `Cron*`/`RemoteTrigger`/`Workflow` tools) and WebGL shader/blend code (~129 hits, a canvas rendering feature). The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `33e28624c5ae…` → patched `35a8d68cd2ca…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature (`codesign --verify`: "valid on disk … satisfies its Designated Requirement"), and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 66238, to `~/.local/share/claude/versions/2.1.205`, inode 58287616) — patched dogfooding. (Disk-level footnote, same shape as 2.1.204: the patched file is only 1,184 bytes smaller than the factory original. The factory 2.1.205 CodeDirectory carries 14,463+7 coarse hash slots plus a 9,045-byte Developer ID certificate chain under a runtime-hardened signature; our ad-hoc CodeDirectory, 14,463+2 slots with no certificate, drops the same certificate chain but few hash-slot bytes — the identical +29-slot, ~1.2 KB-shrink pattern seen at 2.1.204, because this factory build's own signature was already just as lean.) That is the seventeenth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205), none of them a fix.

**Update, 2.1.206 (2026-07-10).** The eighteenth. Claude Code updated 2.1.205 → 2.1.206 — a separate update landing the morning after 2.1.205's, the third daily update in a row (2.1.204 on 2026-07-08, 2.1.205 on 2026-07-09, 2.1.206 on 2026-07-10), and again a consecutive version step, so this two-binary diff brackets no skipped build; 2.1.203 remains the only gap (`~/.local/share/claude/versions/` now holds 204, 205, 206). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.205 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), and accumulator (`a`) — which now makes four consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206) carrying the same local names. Build provenance: the factory binary grew 2,957,056 bytes (237,437,968 → 240,395,024) and the parser site drifted +1,153,245 bytes (210,048,143 → 211,201,388); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade is in play. Anthropic's 2.1.206 changelog (27 entries) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser — it is CLI/UX and plumbing throughout (gateway `/login` endpoints, MCP `request_timeout_ms` honored from `--mcp-config`, `/model` picker row fixes, agents-view fixes, a Bedrock startup hang), with nothing closer to the parser than that. A strings diff (4,548 added / 3,059 removed) is the cleanest yet on the axis that matters: zero charCode/codePoint/tokenizer-flavored lines in the added set (2.1.204 had two, 2.1.205 had ~125 rebuild-resegmentation hits), and the 13 generic `parse` hits all sit elsewhere — a chart runtime's `JSON.parse` calls in dataviz HTML templating, a backend wire-hint comment, a hosts-file regex note. The added set is scattered feature surface — CSS design tokens (70 `--cds-*` lines), chart/dataviz templating (47 hits), agent-flavored strings (47), plugin/LSP plumbing (~24), voice-mode copy (7) — none reaching the parser. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `3197aba4442d…` → patched `d60e80d290a4…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 47004, to `~/.local/share/claude/versions/2.1.206`, inode 58468534, launched three to four minutes after the patch completed at 2026-07-10T00:23:39Z) — patched dogfooding. (Disk-level footnote, back to the 2.1.202 shape rather than 2.1.204/205's: the patched file is 1,398,720 bytes smaller than the factory original. The factory 2.1.206 CodeDirectory returns to the large-slot layout — 58,231+7 hash slots, 1,863,749 bytes, plus a 9,046-byte Developer ID certificate chain — which the ad-hoc re-sign replaces with a 14,558+2-slot CodeDirectory and no certificate; the same mechanical re-sign consequence as before, just against a heavier factory signature this time.) That is the eighteenth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206), none of them a fix.

**Update, 2.1.207 (2026-07-11).** The nineteenth. Claude Code updated 2.1.206 → 2.1.207 — the fourth daily update in a row (2.1.204 on 2026-07-08, 2.1.205 on 2026-07-09, 2.1.206 on 2026-07-10, 2.1.207 on 2026-07-11), again a consecutive version step, so this two-binary diff brackets no skipped build; 2.1.203 remains the only gap (`~/.local/share/claude/versions/` now holds 205, 206, 207). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.206 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), and accumulator (`a`) — which now makes five consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207) carrying the same local names. Build provenance: the factory binary grew 842,112 bytes (240,395,024 → 241,237,136) and the parser site drifted +733,820 bytes (211,201,388 → 211,935,208); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade is in play. Anthropic's 2.1.207 changelog (25 entries) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser. Several entries *sound* adjacent but sit elsewhere: "terminal freezing and keystrokes lagging during streaming of very long lists" is the render layer; "malformed bracket patterns in rules globs … breaking file reads" is file-path glob handling; "crash loop in agent teams from malformed teammate mailbox message" is the agent-teams mailbox; the auto-updater no longer overwriting a custom launcher script or symlink at `~/.local/bin/claude` is update plumbing (this machine's launcher is the stock symlink, and the patch targets the versioned binary it resolves to, so nothing changes for this repo); and "spurious prompt-injection warnings from benign system-generated conversation updates" is a warning-classifier fix in the same *semantic* neighborhood as the cluster's model-side half (§8) but not the parser. A strings diff (3,267 added / 2,721 removed) stays as clean as 2.1.206's on the axis that matters: zero charCode/codePoint/tokenizer-flavored lines in the added set for the second release running, and the 6 generic `parse` hits all sit elsewhere (plugin shell-form warning copy, a script-output parsing note, a proxy-response parser, an ANSI-color `parseFloat`, a Storybook adapter comment, a schedule-interval parsing prompt). The added set matches the changelog's own themes — agent-flavored strings (37), plugin (24), auto-mode (22, the Bedrock/Vertex/Foundry opt-in change), teammate-mailbox (20), worktree (14), launcher/symlink (10) — none reaching the parser. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `1397a062c688…` → patched `da14ef36a630…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 4303, to `~/.local/share/claude/versions/2.1.207`, inode 58669478, launched 23 seconds after the patch completed at 2026-07-11T02:26:36Z) — patched dogfooding. (Disk-level footnote, the large-CD shape again: the patched file is 1,403,616 bytes smaller than the factory original. The factory 2.1.207 CodeDirectory carries 58,435+7 hash slots, 1,870,277 bytes, plus a 9,046-byte Developer ID certificate chain, which the ad-hoc re-sign replaces with a 14,609+2-slot, 467,648-byte CodeDirectory and no certificate — with 2.1.202, 206, and 207 all on the heavy factory signature, 2.1.204/205's lean ones now read as the deviation rather than a new norm.) That is the nineteenth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206), none of them a fix.

**Update, 2.1.208 (2026-07-14).** The twentieth. Claude Code updated 2.1.207 → 2.1.208 — again a consecutive version step, so this two-binary diff brackets no skipped build, though the four-day daily-update streak paused over the weekend (2.1.207 installed Friday 2026-07-11, 2.1.208 Monday 2026-07-14, with upstream's changelog showing no build in between); 2.1.203 remains the only gap (`~/.local/share/claude/versions/` now holds 206, 207, 208). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.207 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), and accumulator (`a`) — which now makes six consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208) carrying the same local names. Build provenance: the factory binary shrank 991,200 bytes (241,237,136 → 240,245,936) — only the second shrink in this ledger after 2.1.204's, this time with no changelog line accounting for it (the release's many memory and disk reductions are runtime RAM and transcript disk, not binary size; at under 1 MB it sits within ordinary build-to-build variation) — and the parser site drifted +252,838 bytes (211,935,208 → 212,188,046); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade is in play. Anthropic's 2.1.208 changelog (45 entries, the longest in this ledger) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser. The entries that *sound* adjacent all sit in the CLI's stream-json I/O protocol — the stdin/stdout JSON-lines framing for SDK hosts and `claude -p` piping — a different layer from the API-response tokenizer VH1 lives in: "truncated stream-json/JSON output … when piping large responses from `claude -p`" is print-mode stdout, "stream-json input killing the session on blank CRLF or whitespace-only lines" is SDK stdin framing, "headless stream-json sessions hanging … [on] a non-string `set_model` payload" is the SDK control protocol, and the Bedrock "Truncated event message received" fix renames a gateway error. One entry belongs in this repo's operational notes rather than the parser ledger: background-session attach failing permanently "after an update replaced the binary a running `claude agents` process was launched from" is upstream fixing a symptom of the same binary-replacement inode decoupling §6 documents — update plumbing, not parsing. A strings diff (6,132 added / 4,329 removed) breaks the two-release streak of zero tokenizer-flavored lines in the added set, but barely: 5 hits, each ruled out on inspection — a named-pipe `charCodeAt(0)` check, a Bun-internal `@String.fromCharCode` trace-event emitter, a TOML-style "Unterminated basic string" error-string table, a Rust-style `input[pos.cast()]` assertion, and a shell-layer "unterminated double quote" — none the character-level JSON string tokenizer; the 26 generic `parse` hits likewise sit elsewhere (a session-store importer skipping unparseable lines, `JSON.parse` over files read from disk, a lazy `parseUrl`, proxy-response and ANSI `parseFloat` code, ripgrep's own error-string table, Bun's bundler `ParseTask`). The added set matches the changelog's own themes — background/daemon/agent strings (96), the new `CLAUDE_CODE_PROCESS_WRAPPER` corporate-launcher surface (43), memory-leak/LRU work (18), transcript/checkpoint pruning (9) — none reaching the parser. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte — the ±260-byte window shows exactly one differing byte; factory `051c7f28871b…` → patched `98652a9956ea…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 66678, to `~/.local/share/claude/versions/2.1.208`, inode 59142150, launched 88 seconds after the patch completed at 2026-07-14T02:32:06Z) — patched dogfooding. (Disk-level footnote, correcting 2.1.207's reading: the re-sign shrank the file only 1,184 bytes because the factory 2.1.208 CodeDirectory is back on the lean layout — 14,634+7 hash slots, 468,645 bytes, plus the usual 9,046-byte Developer ID certificate chain, replaced by a 14,634+2-slot ad-hoc one. With 2.1.202/206/207 heavy and 2.1.204/205/208 lean, the two factory signature layouts now read as alternating rather than the lean ones being a deviation — a mechanical signing detail either way, with no content meaning.) That is the twentieth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207), none of them a fix.

**Update, 2.1.210 (2026-07-15).** The twenty-first. Claude Code updated 2.1.208 → 2.1.210 — skipping 2.1.209 (which was never installed on this machine; `~/.local/share/claude/versions/` now holds 207, 208, 210; upstream's changelog for 2.1.209 is a single entry — a dialog-fix revert — so nothing to bracket), and the four-day daily-update streak that paused over the 2.1.207→208 weekend now has a second gap (2.1.208 Monday 2026-07-14, 2.1.210 Tuesday 2026-07-15). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.208 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), and accumulator (`a`) — which now makes seven consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210) carrying the same local names. Build provenance: the factory binary grew 1,264,032 bytes (240,245,936 → 241,509,968) and the parser site drifted +1,142,615 bytes (212,188,046 → 213,330,661); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade is in play. Anthropic's 2.1.210 changelog (33 entries — mechanical `grep -c`, not the 42 the summarization model hallucinated) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser. The closest-sounding entries all sit in different layers: "Fixed paste markers leaking into external editors … which could appear as stray È/É characters around pasted text" is paste-clipboard encoding, not streaming token parsing; "Hardened the Agent tool against indirect prompt injection via content a subagent read" is a model-side security measure, not a parser fix; and "Fable temporarily shows as unavailable in the advisor picker while a server-side issue causing Fable advisor failures is fixed" is model availability, not parsing. A strings diff (4,193 added / 3,896 removed) returns to zero on the VH1 core axis: no charCode/codePoint/tokenizer-flavored lines in the added set (the 208 release's 5-hit blip did not persist), and the 3 generic `parse` hits all sit elsewhere — a skill-prompt instruction ("the script will parse your output"), a `/loop` interval-parsing prompt template, and a utility function. The 7 `stream` hits are all CLI stream-json I/O and proxy/gateway code, continuing the pattern from 2.1.208. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `1b471d62d111…` → patched `2891b44543c7…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 27421, to `~/.local/share/claude/versions/2.1.210`, inode 59218077, the patched binary completed at 2026-07-15T00:53:33Z) — patched dogfooding. (Disk-level footnote: the patched file is 1,184 bytes smaller than the factory original, matching the lean-CD pattern from 2.1.204/205/208 — the alternating factory signature layouts continue.) That is the twenty-first effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208), none of them a fix.

**Update, 2.1.211 (2026-07-16).** The twenty-second. Claude Code updated 2.1.210 → 2.1.211 — a consecutive version step, so this two-binary diff brackets no skipped build (`~/.local/share/claude/versions/` now holds 208, 210, 211; 2.1.203 and 2.1.209 remain the only builds never installed on this machine). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.210 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), and accumulator (`a`) — which now makes eight consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210, 2.1.211) carrying the same local names. Build provenance: the factory binary grew 935,712 bytes (241,509,968 → 242,445,680) and the parser site drifted +828,635 bytes (213,330,661 → 214,159,296); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade is in play. Anthropic's 2.1.211 changelog (37 entries, mechanical `grep -c` over a direct fetch) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser. The entries that *sound* closest all sit in different layers: "Fixed permission previews relayed to chat channels not neutralizing bidirectional-override, zero-width, and look-alike quote characters, so tool inputs cannot visually alter the approval message" is display-layer anti-spoofing sanitization of the approval prompt — it touches *tool inputs* only at presentation time, not the streaming tokenizer that parses tool arguments; "Improved background agent result reporting — Claude now reports the status of still-running agents and waits for the real completion instead of fabricating results" is a model-side confabulation guardrail, the same root-cause-B territory as 2.1.205's two model-behavior guardrails, not a client parser fix; "Added `--forward-subagent-text` … to include subagent text and thinking in stream-json output" and the fix for headless print-mode sessions crashing "when stdin is unreadable" are CLI stream-json I/O, the same layer as 2.1.208/210; and "Updated integer environment variables (timeouts, token budgets, retry counts) to accept scientific notation … like `1e6` and `64_000`" is env-var integer parsing, not the JSON string tokenizer. A strings diff (3,593 added / 2,958 removed) holds at zero on the VH1 core axis: no charCode/codePoint/tokenizer-flavored lines in the added set (continuing 2.1.210's zero), and the 10 generic `parse` hits all sit elsewhere — the same skill-prompt instruction ("the script will parse your output") and `/loop` interval-parsing template as 2.1.210, plus proxy-response parsing, an ANSI-color `parseFloat`, a Storybook adapter comment, a `parseInt` env-var note, and a `parseToolPreset` helper; the `stream` hits are all CLI stream-json I/O, streaming-doc templates, and proxy/gateway code, continuing the pattern from 2.1.208/210. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte — the ±260-byte window shows exactly one differing byte at the flag; factory `5a728a76198b…` → patched `0a96549030b8…`), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 36759, to `~/.local/share/claude/versions/2.1.211`, inode 59351799, launched 21 seconds after the patch completed at 2026-07-16T01:25:28Z) — patched dogfooding. (Disk-level footnote: the patched file is 1,184 bytes smaller than the factory original, matching the lean-CD pattern from 2.1.204/205/208/210 — the alternating factory signature layouts continue.) That is the twenty-second effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208, 210), none of them a fix.

**Update, 2.1.212 (2026-07-17, backfilled 2026-07-18).** The twenty-third. Claude Code updated 2.1.211 → 2.1.212 — a consecutive version step, so the two-binary diff brackets no skipped build; this entry was written a day after the install, so its claims are split accordingly below. The structural anchor finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature appears 7 times. The parser site is byte-for-byte identical to 2.1.211 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), the ninth consecutive build carrying those names. Build provenance: the factory binary grew 2,084,832 bytes (242,445,680 → 244,530,512) and the parser site drifted +984,913 bytes (214,159,296 → 215,144,209); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.212 changelog (48 entries, mechanical `grep -c` over a direct fetch) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser: the release is session-orchestration surface — `/fork` becoming a background-session copy (with `/subtask` taking the old in-session role), per-session WebSearch and subagent caps, MCP calls auto-backgrounding past 2 minutes — plus a plan-mode permission fix (file-modifying Bash commands now prompt) and a worktree symlink fix, all layers away from the parser. A strings diff (8,523 added / 7,458 removed) shows 141 charCode/codePoint/tokenizer-flavored lines in the added set — the familiar re-segmentation crop on sampling (an ajv schema-codegen template block, a PEM parser, PowerShell launcher strings, highlight.js grammars), none the VH1 site; the added set's themes match the changelog's own (fork/subtask, subagent caps, sandbox, plugin surface). The patcher applied with no script change on install day (`!l`→`!0`, one byte; factory `09ecba2ab2df…` → patched `95f78cf6ed4f…`, file mtimes putting the patch at ~2026-07-17T04:05Z; the state file has since been superseded by 2.1.214's patch). Honest backfill boundary: no `lsof` was captured on install day, so this entry claims no live-inode dogfooding for 2.1.212 — the day-after re-verification covers the on-disk patched binary (bug 0 / fix 1, structural anchor re-run 2026-07-18) and its still-valid ad-hoc signature (CodeDirectory 14,895+2; the factory layout was lean 14,895+7 plus a 9,047-byte Developer ID chain, re-sign shrink the usual 1,184 bytes), and the day-after `lsof` shows no process still holding 2.1.212 — it served its day and was retired by the 2.1.214 update. That is the twenty-third effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208, 210, 211), none of them a fix.

**Update, 2.1.214 (2026-07-18).** The twenty-fourth. Claude Code updated 2.1.212 → 2.1.214 — skipping 2.1.213, which was never installed on this machine and, unlike 2.1.209 (a single-entry changelog), has **no changelog section at all**: npm shows 2.1.213 published 2026-07-17T22:26Z and superseded by 2.1.214 just 107 minutes later (2026-07-18T00:13Z), so there is nothing to bracket and the two-binary diff runs 212 ↔ 214 directly (`~/.local/share/claude/versions/` now holds 211, 212, 214; 2.1.203, 2.1.209, and 2.1.213 are the builds never installed here). The structural anchor still finds exactly one vulnerable site in the factory binary (bug 1 / fix 0), and the `e[++t]` character-scan loop signature still appears 7 times. The parser site is byte-for-byte identical to 2.1.212 — the ±260-byte window matches raw, no normalization needed — still `,!l)r.push({type:"string",value:a})`, same flag (`l`), receiver (`r`), accumulator (`a`), which now makes **ten** consecutive builds (2.1.202, 2.1.204, 2.1.205, 2.1.206, 2.1.207, 2.1.208, 2.1.210, 2.1.211, 2.1.212, 2.1.214) carrying the same local names. Build provenance: the factory binary grew 2,560,992 bytes (244,530,512 → 247,091,504) and the parser site drifted +2,377,380 bytes (215,144,209 → 217,521,589); the Bun banner reads `Bun v1.4.0` unchanged, so no runtime upgrade. Anthropic's 2.1.214 changelog (47 entries, mechanical `grep -c` over a direct fetch) has nothing touching tool-call parsing, JSON tokenization, or the streaming parser. The entries that *sound* closest all sit in different layers: "Fixed stream-json output truncation at exit for slow-reading SDK/pipeline consumers" (the exit drain now scales with queued bytes) is CLI stream-json print-mode stdout, the same layer as 2.1.208/210/211; "Added a periodic progress heartbeat for long-running tool calls" is render-layer UI; the release's headline cluster of permission-check hardening (single-segment `dir/**` rules, fd-redirect forms, >10,000-char commands, zsh subscripts in `[[ ]]`, PowerShell 5.1 bypasses) is the permission engine; and the EndConversation tool is model-facing surface. One entry earns an operational note for this repo rather than a ledger line: "Fixed hooks with exit code 2 not blocking as documented when the hook's stdout JSON fails schema validation" touches the hook engine our PreToolUse guard runs on — but `evap-shield.sh`'s block path prints to **stderr** only and exits 2 with nothing on stdout, so it was never on the broken path and its behavior is unchanged. A strings diff (10,892 added / 11,116 removed — the first entry in this ledger where removals outnumber additions, even as the binary grew 2.4 MB; `strings` counts distinct lines, not bytes) shows 142 charCode/codePoint/tokenizer-flavored lines in the added set, but they are the familiar re-segmentation crop: the same pre-existing library spans surface in both this diff and 2.1.212's (an ajv schema-codegen template block — identifiable across the pair by its `H_` → `w_` rename — a PEM parser, PowerShell launcher strings, highlight.js grammars), plus one named-pipe `name.charCodeAt(0) === 0` check of the same kind ruled out at 2.1.208; the verdict rests on the structural anchor's byte-for-byte window read, not the strings diff. The version-agnostic patcher re-applied with no script change (`!l`→`!0`, one byte; factory `59796dd18e9d…` → patched `69d048ad2b16…`, completed 2026-07-18T04:15:44Z), verified on disk (bug 0 / fix 1), by a valid ad-hoc signature, and by this very session's mmap'd inode (`lsof` resolves this session's own process, PID 1800, to `~/.local/share/claude/versions/2.1.214`, inode 59781012, launched 30 seconds after the patch completed; the machine's one other live claude process, PID 3725, holds the same patched inode) — patched dogfooding. (Disk-level footnote, with a correction to the running narrative: the re-sign shrank the file 1,184 bytes again — factory CodeDirectory 15,051+7 hash slots, 481,989 bytes, plus a 9,046-byte Developer ID certificate chain, replaced by an ad-hoc 15,051+2 — which makes five consecutive lean factory layouts (2.1.208, 210, 211, 212, 214); no heavy layout has appeared since 2.1.207, so the earlier "alternating layouts" reading no longer holds: lean is now simply the norm.) That is the twenty-fourth effective build since 2.1.181 (183, 185, 186, 187, 190, 191, 193, 195, 196, 197, 198, 199, 200, 201, 202, 204, 205, 206, 207, 208, 210, 211, 212), none of them a fix.

**Automating the re-check.** We have since wired this into a `SessionStart` hook (`check-update.sh`) so it runs on its own, because Section 5's cost recurs on every update: each native `claude update` overwrites the patch, raising "did this build fix it, and must I re-patch?" anew. It stays near-instant by not diffing on every launch — a stat-only fingerprint (resolved path, size, mtime) of the live binary gates the work; an unchanged fingerprint means no update happened, so nothing could have been overwritten, and it exits silently. Only a changed binary triggers the full anchored scan from this section, which reports `vulnerable` (re-patch), `patched` (quiet), or `unknown` (pattern gone — possibly fixed upstream, verify). It fails open throughout.

## 8. Honest scope: one corner of the cluster

evap-shield's patch covers Camp A's sub-shape. It is not the whole cluster. The community splits tool-call parsing failures into several sub-patterns, and Camp B's malformed-markup route is a different one. That is exactly why, in our own harness, the patched binary still produced `{}` on a clean truncation: that path hits the secondary fallback, not the primary parser drop (Section 6). The fix is real. It is one corner.

The rest of the landscape is complementary, not competing. `claude-code-unpoison` rewinds a poisoned session after the fact. `cc-safe-setup` classifies incidents and recovers at the hook layer. @in4mer's #67765 is the sharpest root-cause writeup of the bunch. Each covers a piece. evap-shield's piece is patching the one parser sub-shape we could reliably reproduce and verify: no more than that, and that much for real.

**Two weeks on the other side of the line (added 2026-07-03).** Since the first disclosure comment, every incident we hit personally has landed on Camp B's side, not the patched corner: two first-person antml-prefix strikes (2026-06-22 and 2026-06-25 — stray tokens `câ` and `call`, joining the `court`/`count` reported in #64658/#71952/#72352; all c-initial, all sitting where the tool-call opener should be), and one full confabulation session (2026-07-03) in which the model "ran" a smoke test entirely inside a single thinking block, tried to read the nonexistent outputs, and — when the honest "File does not exist" errors came back — reported to the user that the environment was corrupted and that it had twice blocked a "prompt injection". None of that existed in the transcript; every claim failed the claim-vs-artifact check. We could not find a prior report of that last shape (a false security alert delivered as diligence) before our second #62123 comment. None of it is reachable by the patch — which is this section's point sharpened: the cluster's active front has moved to where no client fix can follow, and the useful client-side contribution there is transcript forensics (claim-vs-artifact triage), not more patching.

**Nine days on: the other side is accelerating (added 2026-07-12).** A quantified follow-up on where that front sits. New issues carrying the official `area:model` triage label ran 28/day in April, 21/day in May, 48/day in June, and 93/day over July 1–12 — from 6.5% of all new issues to 26.9% in four months, against a denominator whose daily volume *recovered* in July, so the share is not an artifact of a shrinking base. Within that, issues mentioning Opus 4.8 rose from 19/day (June) to 32/day (July) while their share dipped (39%→35%) as Fable 5 began drawing its own reports (e.g. #76522, the model asserting a false ~15M context window). The shapes are widening too. Fabricated conversation structure — the model inventing user turns, emitting fake `System:`/`Assistant:` scaffolding, confabulating a user request on a cold start's first turn — existed as scattered reports back to March (#38492), surfaced on Opus 4.8 in mid-June (#64260, #70148), and produced five-plus new issues in the second week of July alone (#76784, #76688, #76465, #76378, #73799). And the consequences are escalating from wrong words to wrong actions: #76063 reports the model confabulating tool-output narration, misdiagnosing it as a prompt injection, and deleting data on the strength of its own false alarm — the same false-security-alert shape we documented in the previous paragraph, now carrying a `data-loss` label; #73289 reports an unrequested git commit justified by a quoted user message that exists nowhere in the transcript. Both first-person shapes from that paragraph now have independent counterparts (the false alarm: #76063, #74365; the runaway invisible-thinking stall that opened our 2026-06-24 comment: #76048) — whatever these are, they are not artifacts of our environment. Meanwhile the family's would-be anchor, #64409, was closed by the stale bot on 2026-07-09 (`not_planned`); its candidate successors (#70148, #68990, #64260) sit at a dozen comments or fewer, untouched since late June; and every July issue named above stands at zero reactions. The model-side family has no #62123-style hub concentrating attention, which bodes poorly for prioritization. Two honesty notes on the numbers: they measure *report* rate, not incidence — the visible rise of forensic-styled reports (repro attached, `[MODEL]`-prefixed titles) suggests reporting behavior itself is shifting — and `area:model` is applied by a triage classifier that itself evolves (2.1.207's changelog fixes a warning classifier in this same neighborhood). But the direction is consistent across every proxy we measured, and none of it is reachable from the client. The 2026-07-03 paragraph's conclusion has hardened from an assessment into a measurement: the active front sits where only a model-side or serving-side fix can reach it. Short of that upstream fix, everything on this side of the line reduces to detection, triage, and damage control — patching, ours included, has no purchase here.

## 9. Who runs the verification we couldn't

One sentence in Section 6 is worth returning to: the unreachable end-to-end test is "a structural boundary of the harness, not a missing tool." That is true — and, on its own, it closes a question it should open. Set it beside the other sentence from that section — the primary path "requires the client to actively commit a mid-stream partial buffer, something only the interactive TUI's abort-and-finalize handler does" — and a conclusion the report has carried without stating falls out.

If the primary path fires only in a live TUI session, and no controlled harness can reach it, then the only place the patch's primary-path efficacy can ever be observed is a real session on a real machine. Ours, or yours. The unit test proves the byte-level behaviour is correct (Section 4); it cannot prove the fix lands on the path that bit you in production, because that path does not exist inside the test. So "a structural boundary of the harness" is the honest engineering description, and it is at the same time a transfer: the end-to-end verification we could not run does not disappear, it moves downstream to whoever runs the patched binary. Each person who patches is, on the primary path, the first-line observer of an outcome we never got to watch.

I'd rather name that than leave it in neutral terms. It is not a confession either: the byte-level change is verified at the unit layer (Section 4), the patch is fully reversible with a backup and safety checks (Section 5), and upstream has shipped no fix through 2.1.196 (Section 7). The observer is informed and the risk is reversible. But informed is the precise word, not proven. If you patch, you are not consuming an efficacy demonstrated end to end; on the primary path, you are where that proof finally happens.

## References

- Issues: `anthropics/claude-code` #62123, #67765 (root cause, @in4mer), #63583
- Issues (model-side family): #64658 (stray `court`/`count`, root-cause thread), #64176 (version bisection 2.1.150–158), #71952 / #72015 / #72352 (antml-prefix wave, late June), #64409 (confabulation cluster; closed `not_planned` by the stale bot, 2026-07-09), #64260 / #68990 / #70148 (fabricated-turn family, mid-June), #76784 / #76688 / #76465 / #76378 / #73799 (fabricated-turn wave, early July), #76063 / #74365 (false-injection-alarm shape; #76063 with data loss), #76048 (invisible-thinking stall)
- Disclosure comments on #62123: 2026-06-24 (initial); 2026-07-03 (forensics data: stray-token pattern, confabulation false-alarm shape) — `issuecomment-4878159880`
- evap-shield: `github.com/tznthou/evap-shield` (PreToolUse hook + binary patch)
- Parser patch pattern, 2.1.179: `,!Y)` → `,!0)` (same length, no offset shift). Version-specific: the minified names reshuffle between builds (§7), so locate by the structure `…push({type:"string",value:…})`, not the literal bytes.
- Verification: white-box unit test, 760 truncation cases, 0 regressions
- Binary-diff method: parser-byte anchor + short-string `comm` (Section 7)
