# Claude Code local workflow — never commit
.claude/

# Internal research / experiment trajectory records — local-only
research/

# writing-flow drafts — local-only; only the final docs/ output gets committed
.draft/

# macOS
.DS_Store
._*

# evap-shield runtime logs
*.log
*.log.jsonl

# Editor / IDE
*.swp
*.swo
*~
.vscode/
.idea/

# Backups & temp from patch-vh1.sh / install.sh
*.bak*
*.orig
*.tmp
*.tmp.*
.claude/RESUME.md
